§1〜§3の本文を取得して、各小節の具体的な議論まで踏み込めるようにします。

§1〜§3を、各小節の具体的な議論・例・含意まで踏み込んでまとめます。逆解析の観点で「コードのどこに痕跡が残るか」も各所に短く添えます。

# §1 序論 — 「推論」と「ワークフロー」を分ける

## 1.1 ベイズ推論からベイズワークフローへ

著者はまず3つを峻別します。ベイズ推論とは条件付き確率 p(θ|y) ∝ p(θ)p(y|θ) の定式化と計算にすぎない [arxiv](https://ar5iv.labs.arxiv.org/html/2011.01808) 。これに対しワークフローは、モデル構築・推論・モデルチェック/改善の3ステップに加え、複数モデルの比較を含む。しかもその比較はモデル選択やモデル平均のためだけでなく、より重要なのはモデルを理解するためにある [arxiv](https://ar5iv.labs.arxiv.org/html/2011.01808) ——なぜあるモデルはデータの特定の側面をうまく予測できないのか、なぜ重要パラメータの不確実性がモデル間で変わるのか、を知るためです。気に入ったモデルがあるときでも、より単純なモデルやより複雑なモデルの推論と比較することが、そのモデルが何をしているのかを理解するのに役立つ [arxiv](https://ar5iv.labs.arxiv.org/html/2011.01808) 。

ここで著者は率直な事実を認めます。典型的なワークフローでは一連のモデルをあてはめることになり、その一部は後から見れば下手な選択(データへの不適合、関連理論や実務目標との断絶、弱すぎ/強すぎる事前分布、あるいは単なるプログラミングのバグ)であり、一部は有用だが欠陥がある(一部の交絡因子だけを調整した回帰、関係の一部しか捉えない関数形)、そして一部が最終的に報告に値する [arxiv](https://ar5iv.labs.arxiv.org/html/2011.01808) 。重要なのは、救いようのないモデルや重大な欠陥のあるモデルは、有用なモデルにたどり着くまでの実務上避けられない通過点だ [arxiv](https://ar5iv.labs.arxiv.org/html/2011.01808) という認識で、これが手法の組み立て方そのものを変える、と述べます。なお完全なワークフローはデータ収集前の設計や推論後の意思決定も含むが、本論文はデータのモデリングに絞ります。

> 逆解析への含意:あなたのコードは「最終的に残った一部」だけを見せている可能性が高く、捨てられた中間モデルの痕跡(コメントアウト、未使用の関数、古い事前分布)が「なぜこうなったか」の手がかりになります。

## 1.2 なぜワークフローが必要か

論文は4つの理由を挙げます。

1. **計算が難題だから**——信頼できる推論に到達するには、より単純な/代替モデルのあてはめ、精度は落ちるが速い近似計算、あてはめ過程の探索といったステップを踏む必要がしばしばある。
2. **事前にどのモデルをあてはめるべきか分からないから**——仮に許容可能なモデルが事前に選ばれていても、データが増えたりより詳細な問いを立てたりすれば拡張したくなる。
3. **データが静的でモデルも既知で計算に問題がなくても**、あてはめたモデルとデータの関係を理解したい。その理解は関連モデル群の推論を比較することで最もよく得られる。
4. **異なるモデルが異なる結論を出すのに、どれが優れているとも言えないとき**、複数モデルを提示することがモデル選択の不確実性を示すのに役立つ。

## 1.3 「ワークフロー」の位置づけ

著者は、ワークフローを「例(example)より一般的だが、手法(method)ほど厳密に規定されていないもの」と置きます。tidyverseのような経験的学習の感覚や、機械学習の「プラグアンドプレイ」的な手触り——モデルのあてはめが、確率モデルや統計的仮定への固いコミットではなく、データから何かを学ぶ手段だという健全な感覚——に影響を受けたと述べます。

図2の「メタワークフロー」が要点で、統計手法は **例 → ケーススタディ → ワークフロー → 手法 → 理論** と、左から右へ符号化が進んでいく、という見方です。歴史的な実例として、マルチレベルモデルは経験ベイズの形式化、探索的データ解析は予測的モデルチェックの一形態、lassoやhorseshoeなどの正則化はアドホックな変数選択の置き換え、ガウス過程はカーネル平滑化のベイズ版、と挙げます。いずれも枠組みが既存手法を取り込んで、よりモジュール的になった例です。設定はStanで示すが、他環境にも一般化できる(確率的プログラミング言語は50以上ある)と注記します。

## 1.4 多面的なワークフローをどう整理するか

教科書のワークフローは線形です(臨床試験:検出力計算→収集→クレンジング→解析→p値・信頼区間/経済学の観察研究:EDA→変数変換→回帰→頑健性チェック)。本論文のワークフローはより「もつれて(tangled)」おり、内部に多数のサブワークフローを持ちます。複雑さの源は4つ:

1. **複雑なモデルのあてはめ計算自体が難しく**、目的の事後分布を計算/近似/シミュレートする問題を解くために実験を要し、かつアルゴリズムが意図通り動いたかの確認も要る。
2. **一般には、容易に計算できる以上に複雑なモデル**(相関、階層構造、時間変動パラメータなど)を念頭に置いているため、重要な特徴を欠くと分かっているモデルから始め、計算が楽であることを期待しつつ、徐々に特徴を足していく。
3. **データが固定でないことが多い**(収集が継続中、関連データセットを取り込める)。データ追加はしばしばモデル拡張を要する——最初はうまく合った線形モデルが、新しい条件のデータ追加で破綻する、など。
4. **モデルは代替モデルとの比較で最もよく理解できる**。

底流にあるテーマは「統計は不確実性がすべて」。データやパラメータの不確実性に加え、正しくあてはめられているか、どう設定・拡張すべきか、どう解釈するかについても不確実だ、と。だからワークフローは無秩序になり、データ探索・実質科学・計算・結果解釈の間を行き来する。そして **ショートカット** の扱いが独特です。時間・計算資源・誤りのペナルティの大きさが近道を強いる。著者は「近道を完全ワークフローへの**近似**として明示的に理解する」ことを目指す——そうすれば限られた時間と労力をどこに投じるか賢く選べる。まったくあてはめないより近似的にあてはめる方がましなこともある、と。

## 1.5 本論文の狙いと構成

狙いは、論文や教科書に載らない応用統計の暗黙知を表に出すこと。読者層は (a) 応用ベイズ統計の実務家(特にStan等のPPLユーザー)、(b) 手法/ソフトウェア開発者、そして (c) ベイズ理論・手法の研究者(ワークフローは研究が手薄なため)。構成は、§2(あてはめ前)→§3〜6(あてはめ・デバッグ・評価)→§7(修正)→§8(理解・比較)→§10–11(2つの実例:ゴルフ=新データがモデル改善を促し拡張の予期せぬ困難も示す/惑星運動=計算上の困難がモデリングの問題を指し示す)→§12(考察と批判への応答)。

# §2 モデルをあてはめる前

## 2.1 最初のモデルの選択

ほぼすべての分析の出発点は、**既存の流用**です。類似問題に適用された教科書/ケーススタディ/論文のモデルを採り、改変する——著者はこれをソフトウェア工学の「デザインパターン」になぞらえます。テンプレートを使うことの利点は、結果を見れば「モデル空間のどの方向に有用な精緻化や単純化がありそうか」が分かること。テンプレートは時間を節約し、かつ**結果を理解する人間の認知負荷**も下げます(近道は計算機だけでなく人間にとっても重要で、これがワークフローが反復的になる理由の一つ)。「クックブック分析」という否定的含意はあるが、テンプレートは出発点・比較点として有用だ、と擁護します。戦略は2方向:単純なモデルから始めて特徴を足す、または大きなモデルから始めて、重要な特徴を残しつつ理解可能な単純さまで削ぎ落とす。まったく異なる複数アプローチを並行して始めることもある。

## 2.2 モジュール式の構築

ベイズモデルはモジュールから組み立てられ、各モジュールは**必要に応じて差し替えるプレースホルダー**と見なせます。例:正規分布→裾の重い分布や混合分布、線形の潜在回帰関数→スプラインやガウス過程、観測を厳密値として扱う→測定誤差モデルを追加、弱い事前分布→事後分布が非現実的な値を含むと分かったら強くする。「部品はプレースホルダー」と考えるとモデル構築の重圧が下がる(後でいつでも一般化・追加できるから)。これは、わずかな変更ごとにモデル全体へ新しい名前を付けてきた統計の伝統に逆らう発想で、**モデル全体ではなくモジュールに名前を付ける**方が、一見異なるモデル間のつながりが見え、適応も容易になる、と論じます。

## 2.3 パラメータのスケーリングと変換

パラメータは**解釈可能**であってほしい(実務的にも倫理的にも)。そのため自然なスケールに置き、可能なら独立に(あるいは解釈可能な依存構造で)モデル化し、これが情報のある事前分布の使用を容易にします。スケールを分離して未知量を**スケールフリー**にするのも有効。薬理学の例:値が約50になると見込まれるパラメータを log(θ/50) 上でモデル化すれば、0が解釈可能な値(元スケールの50)に対応し、log上の0.1の差が約±10%に対応する。これは解釈のためだけでなく、**階層モデリングの準備**でもあります——スケールフリーなパラメータの方が部分プーリングが効きやすい。毒性学の例:肝臓の体積を階層モデル化する代わりに「個人の体積×肝臓が占める割合」と表現すると、このスケールフリーな因子は個人間のばらつきが小さく、絶対体積をモデル化するより多くの部分プーリングができる。著者は「スケーリング変換とは、効果的な階層モデリングを可能にする**分解**である」とまとめます。実務的には対数/ロジット変換や標準化(中心を引いてスケールで割る)で概ね単位スケールに乗せる。中心とスケールをデータから計算する場合(rstanarmの既定事前分布など)、それは中心・スケールをハイパーパラメータとして推定する階層モデルの近似と見なせる、と。

> 逆解析への含意:あなたが「理解できない」と感じている**前処理**の多くは、ほぼ確実にこの節の内容です。log/logit/標準化や「比率への分解」は気まぐれではなく、解釈可能性と部分プーリングのための設計判断。前処理コードを見るときは「この変換でパラメータがどんな解釈・スケールになるか」を逆算するのが近道です。

## 2.4 事前予測チェック

**事前分布の含意を生成モデルの文脈で理解する**ための道具。決定的な利点は、観測データではなくモデルからのシミュレーションを使うため、**データを複数回使うことなくモデルを精緻化できる**点です(「データの二度使い」を避けられる)。図3の例が秀逸で、ロジスティック回帰で各係数に独立な normal(0,1) を与え、共変量を2個・4個・15個と増やすと、結果の平均 ȳ の事前予測分布が0か1の近くに集中していく。つまり個々の係数への「弱い」独立事前分布が、予測量 ȳ には「強い」事前分布を意味してしまう。一般則として「予測子が増えるほど、極端な予測を避けるには係数へのより強い事前分布(または十分なデータ)が要る」。対処として、**アウトカムに事前分布を置き、そこから対応するパラメータの同時事前分布を導く**やり方を挙げます(独立事前分布では難しい、大きなパラメータ集合の全体的複雑さの制御ができる)。図4はガウス過程(二乗指数カーネル)で振幅τと長さスケールlを変えた事前予測ドローで、こうした図的比較はどんなモデルでも有用、馴染みのない/複雑なモデルでは必須、と。副次的利点として、観測不能なパラメータへの意見より聞き出しやすい「測定可能な関心量」についての専門家知識の引き出しにも使える。計算を省く場合でも、選んだ事前分布が仮想データにどう影響するかを考える価値はある、と締めます。

> 逆解析への含意:コード内に「データを読み込む前に事前分布からサンプリングして要約・作図する」ブロックがあれば、それがこの節の実装です。設計者がどの予測量の暴走を心配していたかが分かります。

## 2.5 生成モデルと部分的生成モデル

完全なベイズ分析は**生成モデル**(全データと全パラメータの同時分布)を要します。ここに微妙な点がある、と著者は強調します:**ベイズ推論そのものは生成モデルを必要とせず、データから必要なのは尤度だけ**で、異なる生成モデルが同じ尤度を持ちうる。しかし**ベイズ的データ分析**は、予測シミュレーションとモデルチェック(事前/事後予測チェック、擬似データ、SBC、交差検証)のために生成モデルを必要とする。ワークフローは一連の生成モデルを扱うことになる、と。続く部分では、モデルが完全には生成的でない場合(改善事前分布のように事前分布からサンプリングできない、説明変数を生成的にモデル化しない等)を「部分的生成モデル」として論じ、**どこが生成的でどこがそうでないかが、使えるチェック手法を規定する**ことを指摘します。

# §3 モデルをあてはめる

この章は、HMC/NUTS(Stan)を既定としつつ実際に事後分布を計算する段階を扱います(原理は他のサンプラーにも一般化)。逆解析では「サンプラー設定がなぜこうなっているか」に対応します。

## 3.1 初期値・適応・ウォームアップ

MCMCには出発点が要り、HMCではさらにウォームアップ期間にステップサイズと質量行列(metric)を**適応(adaptation)**で調整します。拡散した既定初期値は難しい幾何で問題を起こすことがあり、より妥当な初期値を明示的に与える場合もある。ウォームアップは2つの役割を担う——初期値から「典型集合(typical set)」へ移動すること、そしてサンプラーの調整パラメータを適応させること——で、ウォームアップのドローは破棄します。

> 逆解析への含意:コードに `init=` の指定や `adapt_delta` などの調整があれば、過去に発散/収束不良に遭った痕跡である可能性が高いです。

## 3.2 反復アルゴリズムをどれだけ走らせるか

**異なる初期値から複数チェーンを走らせ**、収束を **R̂**(チェーン間分散とチェーン内分散の比、1に近いことを求める)で、推定精度を **有効サンプルサイズ(ESS、本体用と裾用)**で評価します。関心量に対してR̂が十分1に近く、ESSが十分大きくなったら「十分」と判断する。ここで重要なのは、サンプラーの収束は良いモデルの**必要条件であって十分条件ではない**——よく混ざっても、モデルがデータに合っているとは限らない、という点です。

## 3.3 近似アルゴリズムと近似モデル

2種類の近似を区別します。**アルゴリズムを近似する**(変分推論、ラプラス近似/INLA、EP、Pathfinder、ADVIなど。速いが無限計算時間でも厳密にはならない)か、**モデルを近似する**(モデル自体を単純化する)か。トレードオフは速度・スケーラビリティ対精度。反復の初期段階(「速くあてはめる」)では有用だが、近似推論は誤導しうるので、小規模問題で厳密手法と突き合わせて検証すべき。段階ごとに適した近似が異なる、と。

> 逆解析への含意:コードがVI/ラプラス等を使っているなら、それは「速く回す」ための妥協かもしれません。MCMCでの検証が一度でもされたか(あるいは省かれたか)を確認する価値があります。

## 3.4 速くあてはめ、速く失敗する(Fit fast, fail fast)

章を貫く原理です。モデルが**間違っている/あてはまらないことを素早く発見**できれば反復が速く回る。そこで、データの部分集合、単純化したモデル版、速い近似計算を使って、バグ・モデル誤特定・識別不能を**早期に**検出できるパイプラインを組む。これは反復的モデリングの哲学と、後の§5(「計算の困難はしばしばモデルの問題を指す」というfolk theorem)に直結します。

---

ここまでが§1〜§3です。逆解析の文脈で要点を一言にすると、**§2.3と§2.4があなたの「前処理が分からない」に、§3が「なぜこのサンプラー/推定設定なのか」に直接対応**します。

次は§4(擬似データ・SBC・構築データでの実験——「入力データの検定」を実装で見分ける鍵)、または§5(計算問題の処方箋10種)を、同じ深さで続けられます。どちらにしますか。

§4〜§7を、§1〜§3と同じ深さで続けます。§6は特に、あなたが「理解できない」と言っていた**後処理**に直接対応します。

# §4 構築データで問題を見つけ理解する

この章のテーマは「実データに触れる前に、**自分で作ったデータ**でモデルとコードを検証する」ことです。あなたが挙げた「入力データの検定」の正体の多くはここにあります。

## 4.1 擬似データシミュレーション(fake-data simulation)

最も基本的な検証。既知のパラメータ値を決め、そこから生成モデルでデータを合成し、そのデータにモデルをあてはめて**真値を回収できるか**を確認します。回収できなければ、原因はプログラミングのバグ、パラメータの識別不能、あるいはデータ量に対してモデルが複雑すぎることのいずれか。単一の真値だけでなく、真値を変えながら繰り返すと「どの条件なら何が学習できるか」が見えます。重要なのは、これが**推論の正しさ(コードが事後分布を正しく計算しているか)**と**モデルの識別性**を切り分ける手段だという点です。

## 4.2 シミュレーションベース較正(Simulation-Based Calibration, SBC)

擬似データ検証をより厳密にしたもので、Talts らによる手法(Cook–Gelman–Rubin の検証法の発展)。手順は、(1) 事前分布からパラメータ θ を引く、(2) そこからデータを生成する、(3) そのデータで推論し事後分布を得る、(4) 真値が事後分布の中で占める順位(分位点)を記録する——これを多数回繰り返します。**推論が正しければ、その順位は一様分布になる**はずで、一様からのずれが系統的なバイアス(事後分布が広すぎる/狭すぎる/偏っている)を示します。擬似データ検証が「ある一点で回収できるか」を見るのに対し、SBC は**事前分布全体にわたってモデル+アルゴリズムが整合的か**を検査する点が強力です。

## 4.3 構築データを用いた実験

検証を超えて、合成データを**理解の道具**として使う段階。極端なケース(データ量を増減させる、効果の大きさを変える、外れ値を入れる)のデータを作ってモデルの挙動を観察し、「このモデルは何を捉え、何を捉えられないか」「どのパラメータが弱く識別されるか」を探ります。実データの結果を解釈する前の「感度の地図」を作る作業です。

> 逆解析への含意:コードに「真のパラメータを決め→データを生成→あてはめ→回収を確認」や「事前分布から引く→順位を集計」というループがあれば、それぞれ擬似データ検証/SBCの実装です。これらは設計者が**どの推定を信頼してよいと判断したか**の根拠であり、「なぜこの事後分布を採用したか」に直接答えます。

# §5 計算上の問題への対処

10の小節からなる、論文中もっとも実務的な処方箋集。逆解析では「なぜこんな書き方/パラメータ化になっているのか」の答えがここに集中しています。

## 5.1 計算のfolk theorem(民間定理)

この章を貫く中心思想。「計算がうまくいかないとき、その原因はしばしば計算ではなく**モデル自体**にある」。発散・混ざりの悪さ・遅さを単なる数値的不具合として片付けず、モデル誤特定や識別不能のシグナルとして読む、という姿勢です。

## 5.2 単純なモデルと複雑なモデルから始め、中間で合流

目的のモデルへ、**単純なモデルから機能を足して**近づくと同時に、**完全なモデルから機能を削って**近づき、両者を突き合わせます。どちらか一方だけだと、どこで壊れたか/どこが効いているかが分からなくなるため。

## 5.3 あてはめに時間がかかるモデルの扱い

遅いモデルは反復を殺すので診断します。データの部分集合や単純化版で速く回し、どの要素が遅さの原因か(尤度評価か、幾何の難しさか)を切り分け、ボトルネックに対処する。§3.4「Fit fast, fail fast」の実践版です。

## 5.4 中間量のモニタリング

ブラックボックスにせず、内部の量(線形予測子、変換後パラメータ、対数尤度の各項など)を出力・保存して、**どこで破綻しているかを局所化**します。Stanなら生成量ブロックで中間値を吐き出す、といった手段。

## 5.5 混ざりの悪いチェーンのスタッキングによる再重み付け

複数チェーンが事後分布の異なる領域に留まってしまうとき(混ざらないとき)、各チェーンに**スタッキングの重み**を付けて統合する手法(Yao らの研究)。完全な収束が得られない状況でも、予測性能に基づいて妥当な推論を取り出す現実的な手立てです。

## 5.6 多峰性や難しい幾何

事後分布の多峰性を、**良性**(対称性・ラベル交換など、実質的に同等な峰)と**問題のある多峰性**(異なる実質的解に対応)に区別します。あわせて、階層モデルの「漏斗(funnel)」のような難しい幾何や裾の重さがサンプリングを困難にすることを論じます。

## 5.7 再パラメータ化

数学的に等価だがサンプラーにとって扱いやすい座標に書き換える技法。代表例が階層モデルの**非中心化パラメータ化(non-centered parameterization)**で、群レベルの分散が小さいときに生じる漏斗を解消します。事後分布の中身は変えずに幾何だけを改善します。

## 5.8 周辺化

離散パラメータや厄介な潜在変数を**解析的に積分消去**し、HMCが扱いやすい連続のみの問題にする技法(HMCは離散パラメータを直接サンプルできないため特に重要)。混合モデルや隠れ状態モデルでよく使われます。

## 5.9 事前情報の追加

弱情報事前分布(weakly informative priors)を加えて、事後分布を非現実的な領域から引き戻し、計算を安定させます。これは「データを歪める」のではなく、識別不能や発散を**正則化**で解消する設計判断です。

## 5.10 データの追加

追加データや追加のデータ源で、弱く識別されているパラメータを制約します。モデルではなく情報を足すことで問題を解く方向です。

> 逆解析への含意:あなたのコードに非中心化(`_raw` 付きパラメータ×スケール+位置の形)、尤度の `log_sum_exp` による周辺化、あるいは一見「強め」の事前分布があれば、それらは**過去に計算が壊れた証拠**です。folk theoremの観点では、これらの対処の存在自体が「モデルのどこに緊張があったか」を逆に教えてくれます。

# §6 あてはめたモデルの評価と利用

ここがあなたの言う「**後処理**」の中核です。あてはめた後に「そのモデルでよいか」「結果をどう要約し不確実性をどう伝えるか」を扱います。

## 6.1 事後予測チェック(posterior predictive checking)

事後分布から**複製データ**を生成し、観測データと比較して不適合を見つけます。検定量(平均・分散・分位点・特定のサブグループの挙動など、関心のある側面)やグラフィックスで「モデルが再現できていない特徴」を探す。事前予測チェック(§2.4)が「データを見る前の妥当性」を見たのに対し、これは「データに照らした適合」を見ます。

## 6.2 交差検証と個々のデータ点・部分集合の影響

予測性能を**交差検証**で評価します。実務上は PSIS-LOO(Pareto平滑化重点サンプリングによる一個抜き交差検証。Vehtari・Gelman・Gabry ら)で、再フィットせずにLOOの対数予測密度(elpd)を近似する。あわせて、**どのデータ点や部分集合がモデルに強い影響を与えているか**(外れ値、影響点)を、Paretoの k 診断などで特定します。これは「結論がどの観測に支えられているか」を明らかにします。

## 6.3 事前情報の影響

**事前分布感度分析**。事前分布を変えたときに結論がどれだけ動くかを調べ、「結果がデータ由来か事前分布由来か」を切り分けます。事前分布が結論を強く駆動しているなら、それを明示する必要がある、という姿勢です。

## 6.4 推論の要約と不確実性の伝播

最終段階。事後分布を要約し(点推定だけでなく区間・分布として)、その不確実性を**下流の予測や意思決定まで伝播**させます。点推定で潰さずに分布のまま運ぶことが、ベイズの利点を活かす後処理だ、という主張です。

> 逆解析への含意:コードの「後処理」部分に複製データの生成と比較、`loo`/`elpd`/Pareto-k の計算、事前分布を差し替えた再実行があれば、それぞれ §6.1 / §6.2 / §6.3 です。**重要度分解の出力**も、多くはこの「要約と不確実性の伝播」(§6.4)の一部として位置づけられます。

# §7 モデルの修正

評価で見つかった不足を受けて、モデルを反復的に作り変える段階。ワークフローが「直線」ではなく「ループ」である所以です。

## 7.1 データに対するモデルの構築

チェックの結果に応じてモデルを拡張・改変していく実務。§2の「プレースホルダーを差し替える」発想が、ここで評価結果に駆動されて具体化します(裾を重くする、非線形項を入れる、群変動を許す、など)。

## 7.2 追加データの取り込み

新しいデータや別のデータ源を取り込む際、しばしばモデルの拡張が必要になります(パラメータを群ごとに変動させる、関数形を拡げる)。§1.4で触れた「線形モデルが新条件のデータで破綻する」状況への対処です。

## 7.3 事前分布を扱う

事前分布を**一度決めたら終わり**にせず、反復の対象として扱います。事前予測チェック(§2.4)や事前分布感度分析(§6.3)の結果を踏まえ、弱情報事前分布をどう設定・調整するか、スケールにどう合わせるかを再検討する。事前分布は計算(§5.9)とモデリングの両方に関わる調整点だ、という整理です。

## 7.4 モデルのトポロジー

締めくくりの概念。検討した一連のモデルは孤立した点ではなく、**互いに連結した「モデルの空間(ネットワーク)」**をなす、という見方です。あるモデルから別のモデルへは特徴の追加・削除で「移動」でき、ワークフローとはこの空間を航行する営みだと位置づけます。これが§8(複数モデルの理解・比較)への橋渡しになります。

---

ここまでが§4〜§7です。要点を逆解析に引きつけると、**§4があなたの「入力データの検定」、§6があなたの「後処理」と「重要度分解の出力」に直接対応**し、§5は「なぜこの実装になっているか(過去に何が壊れたか)」の答えが詰まっています。

残るは§8(複数モデルの可視化・交差検証・モデル平均)、§9(モデリングをソフトウェア開発として扱う——「制御できない」状態への最も実践的な処方)、§10–11(2つの実例)、§12(考察と批判への応答)です。続けますか。§9はあなたの当初の悩みに最も近いので、先に単独で扱うこともできます。

§8〜§11を続けます。§8〜§9は手法・実務、§10〜§11は2つの実例で、これまでの全章が実際にどう組み合わさるかを見せる部分です。

# §8 複数モデルの理解と比較

§7で「モデルは連結した空間をなす」と述べた流れを受け、**一連のモデルをどう見比べ、どう使うか**を扱います。

## 8.1 モデルどうしの関係を可視化する

複数モデルの推論を**互いの関係として図示**します。各モデルのパラメータ推定や予測を並べ、「あるモデルから別のモデルへ移ると、どの推定が・なぜ変わるか」を見る。単一モデルの結果を眺めるより、**比較によってこそ各モデルが何をしているかが分かる**(§1.1の主張の実践)。たとえば、ある共変量を加えると別の係数の不確実性が縮む/広がる、といった関係を視覚化します。

## 8.2 交差検証とモデル平均

モデル比較の基準を**予測性能(交差検証、§6.2のLOO/elpd)**に置きます。重要なのは、著者が「単一の最良モデルを選ぶ」ことに慎重で、**ベイズ的スタッキング(Yao ら)**によるモデル平均を推す点です。複数モデルの予測を、交差検証性能に基づく重みで組み合わせる。「正しいモデルが候補内にある」と仮定するベイズモデル平均(BMA)よりも、モデルが**すべて近似**である現実(=M-open設定)に適した統合法だ、という整理です。

## 8.3 多数のモデルを比較する

モデルが多数になると、**比較自体への過適合**が問題になります。多くのモデルを交差検証で比べて「最良」を選ぶと、選択がノイズに過適合しうる。elpd差の標準誤差を見て差が意味的かを判断する、といった注意を促します。これは§12.3(モデル選択と過適合)への伏線です。

> 逆解析への含意:コードが複数モデルをあてはめて elpd を比較していたり、スタッキング重みで予測を混ぜていたりすれば、それは§8です。設計者が「単一モデルを選ばず不確実性を残す」判断をした証拠で、「なぜ複数の事後分布が併存しているのか」に答えます。

# §9 モデリングをソフトウェア開発として扱う

あなたの当初の悩み——「もう制御できない」——に最も近い章です。著者の主張は単純で、**統計モデリングをソフトウェア工学として運用せよ**。逆に言えば、この章の規律が欠けると、まさにあなたの状況に陥ります。

## 9.1 バージョン管理

モデルとコードをバージョン管理することで、**他者および「過去の自分」との協働**が滑らかになります。「なぜこの事後分布になったか」は、しばしば過去のコミット履歴(どの順でモデルを変えたか)に書かれている。履歴がなければ、§1.1で述べた「捨てられた中間モデルの系列」が完全に失われ、意図の復元が困難になります。

## 9.2 逐次的なテスト(testing as you go)

書きながらテストする。§4の擬似データ検証やSBCを**回帰テスト**として組み込み、モデルを変えるたびに「以前は通っていた検証がまだ通るか」を確認する。これにより、変更がどこを壊したかを即座に局所化できます。

## 9.3 本質的に再現可能にする

他者(将来の自分を含む)が同じ結果を再現できるようにする。乱数シード、データのバージョン、依存パッケージのバージョン、実行環境を固定する。再現できないコードは、検証も逆解析もできません。

## 9.4 可読で保守可能にする

意味のある変数名、モジュール化(§2.2の発想をコードにも適用)、コメントによる**意図**の記録。コードが「何をするか」だけでなく「なぜそうするか」を残す。

> 逆解析への含意:この章はあなたの状況の**原因と処方箋の両方**です。原因——元のコードに§9の規律(履歴・テスト・再現性・可読性)が欠けていたから制御不能になった。処方箋——逆解析とは、事後的にこの規律を取り戻す作業に他なりません。具体的には、(1) まず§4のテスト(擬似データで現状の挙動を固定)を書いて「壊さずに触れる」土台を作り、(2) §2〜§6の各段階に対応するコード片を特定して意図をコメントとして注記し、(3) バージョン管理下に置いてから改変を始める、という順序が安全です。

# §10 実例:ゴルフのパッティング(モデルの構築と拡張)

ここからは、これまでの章が**実際にどう連鎖するか**の物語です。ゴルフの例は「新しいデータがモデル改善を促し、拡張が予期せぬ困難を生む」筋立て(元は Gelman 2019)。

## 10.1 最初のモデル:ロジスティック回帰

成功確率を距離 x の関数として logit(p) = a + b·x でモデル化。データには概ね合うが、**純粋に記述的**で、係数 a, b に物理的意味がなく、距離を外挿すると不自然な予測になりうる。「合うが理解できない」モデルの典型です。

## 10.2 第一原理からのモデリング

物理(幾何)からモデルを導きます。ゴルファーはカップを狙うが角度に正規誤差(標準偏差 σ)がある。ボールが入るのは角度のずれが閾値 arcsin((R−r)/x) 以内のとき(R はカップ半径≈2.125インチ、r はボール半径≈0.84インチ、x は距離)。よって P(成功|x) = 2Φ(arcsin((R−r)/x)/σ) − 1。**パラメータはσ一つだけ**で、しかも「狙いの角度誤差」という解釈を持つ。これが元データに、より少ないパラメータでより良く合います。ロジスティック回帰との対比が、**機構モデルの価値**(少ない解釈可能なパラメータ・外挿の妥当性)を示します。

## 10.3 あてはめたモデルを新しいデータで検証

より広い距離範囲を含む大規模データ(Broadie のデータ)で検証すると、幾何モデルが**長距離で破綻**します。遠distanceでは支配的な誤差が「角度」ではなく「距離」になるから——強く打ちすぎればカップを通り過ぎ、弱ければ届かない。§6の「新データでの検証」が、モデルの隠れた限界を露呈させた瞬間です。

## 10.4 打つ強さを考慮した新モデル

第二の誤差源(**距離・打力の誤差**)を加えます。ボールが入るには、角度が十分正確で、かつ打った距離がカップに届きつつ行き過ぎない範囲(ある「オーバーシュート許容量」内)に収まる必要がある。打つ距離を「意図した距離×(1+誤差)」のように誤差付きでモデル化し、角度と距離の二つの成功条件を組み合わせます。§7の「モデルの構築・拡張」の実演です。

## 10.5 ファッジ因子(fudge factor)による拡張

二誤差モデルでも、観測のばらつきが二項分布の標本変動より大きい(**過分散**)という系統的な不適合が残ります。そこで分散に小さな**調整項(ファッジ因子)**を加え、モデルが捉えきれない変動を明示的に吸収させます。これは「ごまかし」ではなく、**不確実性を正直にする**ための拡張です。

## 10.6 ゴルフの例からの一般的教訓

機構モデルの価値、新データと予測チェックに駆動される反復的拡張、そして「モデルは近似」という前提のもとでファッジ因子により不確実性を誠実に表現すること。拡張は問題を解くと同時に**新たな困難を生む**こともある、という現実も示されます。

# §11 実例:予期せぬ多峰性(惑星運動)

二つ目の例は逆向きの教訓——「**計算上の困難がモデリングの問題を指し示す**」(folk theorem の実演。元は Margossian and Gelman 2020)。

## 11.1 運動の機構モデル

惑星(軌道)運動を、重力に基づく**常微分方程式(ODE)**で記述する機構モデル。初期条件や力学パラメータから軌道を解き、観測位置に対する尤度を構成します。

## 11.2 単純化したモデルのあてはめ

まず単純化した版をあてはめます(§5.2「単純な側から始める」の実践)。

## 11.3 「悪いマルコフ連鎖か、遅いマルコフ連鎖か?」

ここが核心の問い。サンプリングが不調(混ざらない・遅い・発散する)に見えるとき、それは**サンプラーが壊れている(悪い連鎖)のか、単に時間が足りない(遅い連鎖)のか、それともモデル自体の問題なのか**を切り分けます。診断の結果、事後分布に**予期せぬ多峰性**が見つかる——複数の峰は、軌道パラメータの縮退や周期性に起因する識別性の問題でした。計算の不調が、実は実質的なモデルの構造を露わにしたわけです。

## 11.4 モデルの構築(積み上げ)

見つかった多峰性に、再パラメータ化・事前情報・制約の追加(§5.7・§5.9)で対処し、単純な側から機能を積み上げて、サンプリング可能で識別されたモデルへ到達します。

## 11.5 惑星運動の例からの一般的教訓

**計算診断はモデル診断でもある**。R̂やESSの異常、発散を「数値的不具合」として潰さず、モデルの識別性・幾何のシグナルとして読むこと。§5.1のfolk theoremが、具体例を通じて裏づけられます。

> 逆解析への含意:この2例は、コードを読むときの「**意図の地層**」の読み方を教えます。ゴルフ型——コード内のモデルが「単純→機構的→誤差源の追加→ファッジ因子」と段階を踏んでいれば、新データや不適合に駆動された拡張の跡。惑星型——非中心化・制約・強めの事前分布が後付けされていれば、過去に多峰性や発散に遭った跡。どちらも、各コード片を**ワークフローのどの段階の対処か**に対応づければ「なぜこうなったか」が復元できます。

---

ここまでで§4〜§11が揃いました。残るは§12(考察)で、(1) 統計モデリングの諸立場、(2) **反復的モデル構築の正当化**(データを見てモデルを直すと推論が無効になるのでは、という批判への核心的応答)、(3) モデル選択と過適合、(4) 大きなデータは大きなモデルを要求する、(5) 予測・一般化・事後層別化、(6) 今後の展望、です。

§12を続けますか。特に12.2は、あなたが逆解析の末に「このコードのやり方は統計的に正当化できるのか」を判断するうえで効く議論です。