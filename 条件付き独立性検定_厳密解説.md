# 条件付き独立性検定：厳密解説

作成日: 2026-06-09

## このノートの目的

このノートは、PCMCI / FullCI / PCアルゴリズムで中心になる **条件付き独立性検定** を、数式ベースで厳密に理解するための補助ノートです。

一言でいうと、条件付き独立性検定は、

> $Z$ を知ったあとでも、$X$ と $Y$ にまだ統計的な関係が残るか？

を検定する手続きです。

関連ノート:

- [[条件付き独立性]]
- [[FullCI_数式ベース厳密解説|FullCI：数式ベースの厳密解説]]
- [[CMI_GPDC_ParCorr|CMI / GPDC / ParCorr]]
- [[偏相関と検出力]]
- [[PCアルゴリズム]]
- [[../01. 論文読解/03_式から読む|PCMCI 式から読む]]
- [[../01. 論文読解/04_手法の流れ|PCMCI 手法の流れ]]

---

## 1. まず「独立」とは何か

確率変数 $X$ と $Y$ が独立であるとは、同時分布が周辺分布の積に分解できることです。

$$
p(x,y) = p(x)p(y)
$$

このとき、

$$
X \perp Y
$$

と書きます。

| 記号 | 意味 |
|---|---|
| $X$ | 1つ目の確率変数 |
| $Y$ | 2つ目の確率変数 |
| $p(x,y)$ | $X=x$ かつ $Y=y$ の同時分布 |
| $p(x)$ | $X=x$ の周辺分布 |
| $p(y)$ | $Y=y$ の周辺分布 |
| $X \perp Y$ | $X$ と $Y$ は独立 |

直感的には、$Y$ を知っても $X$ の予測が変わらない、ということです。

同値な書き方として、

$$
p(x \mid y) = p(x)
$$

または

$$
p(y \mid x) = p(y)
$$

があります。

---

## 2. 条件付き独立とは何か

条件付き独立は、第三の変数 $Z$ を知ったあとでの独立性です。

$X$ と $Y$ が $Z$ の下で条件付き独立であるとは、ほとんどすべての $z$ について、

$$
p(x,y \mid z) = p(x \mid z)p(y \mid z)
$$

が成り立つことです。

このとき、

$$
X \perp Y \mid Z
$$

と書きます。

同値に、

$$
p(x \mid y,z)=p(x \mid z)
$$

または

$$
p(y \mid x,z)=p(y \mid z)
$$

とも書けます。

日本語では、

> $Z$ を固定して比較すると、$X$ と $Y$ の間に追加の関係はない

という意味です。

---

## 3. 条件付き独立性検定の仮説

条件付き独立性検定では、次の帰無仮説と対立仮説を立てます。

$$
H_0: X \perp Y \mid Z
$$

$$
H_1: X \not\perp Y \mid Z
$$

| 仮説 | 数式 | 意味 |
|---|---|---|
| 帰無仮説 | $H_0: X \perp Y \mid Z$ | $Z$ を知れば、$X$ と $Y$ に追加関係はない |
| 対立仮説 | $H_1: X \not\perp Y \mid Z$ | $Z$ を知っても、$X$ と $Y$ に関係が残る |

重要なのは、検定は通常、**独立であることを証明する**のではなく、

> 独立であるという仮説を棄却できるか

を調べる点です。

$p$値が小さいとき、たとえば

$$
p\text{-value} < \alpha
$$

なら、$H_0$ を棄却し、

$$
X \not\perp Y \mid Z
$$

と判断します。

一方で、$p$値が大きいときは、

$$
X \perp Y \mid Z
$$

を証明したのではなく、

> 今のサンプルサイズと検定方法では、条件付き依存を検出できなかった

と読むのが厳密です。

---

## 4. なぜ条件付き独立が因果探索に効くのか

### 4.1 共通原因の例

次の構造を考えます。

```text
Z -> X
Z -> Y
```

このとき、$X$ と $Y$ は相関します。

$$
X \not\perp Y
$$

しかし、共通原因 $Z$ を条件にすると、$X$ と $Y$ の関係は消えることがあります。

$$
X \perp Y \mid Z
$$

つまり、$X$ と $Y$ の相関は、直接因果ではなく $Z$ による見かけの関係だった、と読めます。

### 4.2 間接経路の例

次の構造を考えます。

```text
X -> Z -> Y
```

このとき、$X$ と $Y$ は無条件では依存します。

$$
X \not\perp Y
$$

しかし、中間変数 $Z$ を条件づけると、直接の情報経路が遮断されます。

$$
X \perp Y \mid Z
$$

このため、条件付き独立性検定を使うと、

> $X$ と $Y$ の関係は直接リンクなのか、それとも中間変数を通じた間接関係なのか

を区別しやすくなります。

### 4.3 colliderの例

次の構造を考えます。

```text
X -> Z <- Y
```

このとき、通常は $X$ と $Y$ は無条件で独立です。

$$
X \perp Y
$$

しかし、colliderである $Z$ を条件づけると、逆に依存が生じます。

$$
X \not\perp Y \mid Z
$$

これは条件付き独立性を読むときに非常に重要です。

条件づけは常に「余計な関係を消す」わけではありません。colliderに条件づけると、存在しなかった依存を開いてしまうことがあります。

---

## 5. 確率分布による厳密定義

離散変数の場合、条件付き独立は次で定義されます。

$$
P(X=x, Y=y \mid Z=z)
=
P(X=x \mid Z=z)P(Y=y \mid Z=z)
$$

すべての $x,y,z$、ただし $P(Z=z)>0$ について成り立つとき、

$$
X \perp Y \mid Z
$$

です。

連続変数の場合は密度関数で、

$$
f_{X,Y\mid Z}(x,y\mid z)
=
f_{X\mid Z}(x\mid z)f_{Y\mid Z}(y\mid z)
$$

が、ほとんどすべての $x,y,z$ で成り立つことを意味します。

また、条件付き分布で書けば、

$$
f_{Y\mid X,Z}(y\mid x,z)=f_{Y\mid Z}(y\mid z)
$$

です。

これは、

> $Z$ が分かっているなら、$X$ を追加で知っても $Y$ の分布は変わらない

という意味です。

---

## 6. 条件付き相互情報量による検定

条件付き独立性を測る一般的な量が、条件付き相互情報量です。

$$
I(X;Y\mid Z)
=
\int p(x,y,z)
\log
\frac{p(x,y\mid z)}{p(x\mid z)p(y\mid z)}
\, dx\, dy\, dz.
$$

離散変数なら、積分の代わりに和で書けます。

$$
I(X;Y\mid Z)
=
\sum_{x,y,z} p(x,y,z)
\log
\frac{p(x,y\mid z)}{p(x\mid z)p(y\mid z)}.
$$

重要な性質は、

$$
I(X;Y\mid Z) \ge 0
$$

であり、さらに適切な正則条件のもとで、

$$
I(X;Y\mid Z)=0
\quad \Longleftrightarrow \quad
X \perp Y \mid Z
$$

です。

つまり、CMIベースの条件付き独立性検定では、

$$
H_0: I(X;Y\mid Z)=0
$$

を検定します。

PCMCIでのFullCIなら、

$$
X = X^i_{t-\tau},
\quad
Y = X^j_t,
\quad
Z = \mathbf{X}^{-}_{t,\tau_{\max}} \setminus \{X^i_{t-\tau}\}
$$

と置いて、

$$
I\left(X^i_{t-\tau};X^j_t \mid \mathbf{X}^{-}_{t,\tau_{\max}} \setminus \{X^i_{t-\tau}\}\right)
$$

を調べます。

---

## 7. 偏相関による検定：線形ガウスの場合

線形ガウスの場合、条件付き独立性は偏相関で調べられます。

$$
X \perp Y \mid Z
\quad \Longleftrightarrow \quad
\rho_{XY\cdot Z}=0
$$

ただし、この同値は主に多変量正規分布などの仮定の下で成り立ちます。

### 7.1 残差化による定義

まず、$X$ を $Z$ で線形回帰します。

$$
X = a_X^\top Z + r_X
$$

次に、$Y$ を $Z$ で線形回帰します。

$$
Y = a_Y^\top Z + r_Y
$$

| 記号 | 意味 |
|---|---|
| $a_X$ | $Z$ から $X$ を予測する係数 |
| $a_Y$ | $Z$ から $Y$ を予測する係数 |
| $r_X$ | $Z$ では説明できなかった $X$ の残差 |
| $r_Y$ | $Z$ では説明できなかった $Y$ の残差 |

偏相関は、残差同士の相関です。

$$
\rho_{XY\cdot Z}
=
\operatorname{corr}(r_X,r_Y).
$$

つまり、偏相関による条件付き独立性検定は、

> $Z$ で説明できる部分を取り除いたあとでも、$X$ と $Y$ の残差が相関しているか？

を見ています。

### 7.2 $t$検定による偏相関検定

サンプルサイズを $n$、条件集合 $Z$ の次元を $k$ とします。

標本偏相関を $r_{XY\cdot Z}$ とすると、帰無仮説

$$
H_0: \rho_{XY\cdot Z}=0
$$

のもとで、典型的には次の統計量を使います。

$$
t
=
r_{XY\cdot Z}
\sqrt{
\frac{n-k-2}{1-r_{XY\cdot Z}^2}
}.
$$

自由度は、

$$
\nu = n-k-2
$$

です。

この式から分かる重要点は、条件集合の次元 $k$ が大きくなると、自由度 $n-k-2$ が小さくなることです。

したがって、高次元条件づけでは、同じ偏相関でも有意になりにくくなります。

これはFullCIの検出力が落ちる主要理由の1つです。

---

## 8. カーネル・距離・非線形CI検定

偏相関は線形関係に強い一方で、非線形依存を見落とすことがあります。

そこで、非線形の条件付き独立性検定として、たとえば次のような方法があります。

| 方法 | ざっくり何を見るか | PCMCIとの関係 |
|---|---|---|
| CMI | 条件付き相互情報量 | 一般的だが高次元で推定が難しい |
| GPDC | Gaussian Process Distance Correlation | PCMCI論文で非線形CI検定として使用 |
| KCI | Kernel Conditional Independence test | カーネルで非線形依存を検出 |
| CMIknn | k近傍法によるCMI推定 | Tigramiteなどで使われることがある |

非線形CI検定の難しさは、特に条件集合 $Z$ の次元が高いときに出ます。

$$
\dim(Z) \uparrow
\quad \Rightarrow \quad
\text{推定が難しくなる}
$$

これが、FullCIのように全過去を条件集合に入れる方法で問題になります。

---

## 9. 条件付き独立性検定の一般手順

一般的な条件付き独立性検定は、次の流れです。

```text
入力:
  観測データ {(x_t, y_t, z_t)}_{t=1}^n
  条件付き独立性検定の方法 CItest
  有意水準 alpha

1. 帰無仮説 H0: X ⟂ Y | Z を立てる
2. 検定統計量 T_n を計算する
3. H0 のもとでの T_n の分布を使って p値を計算する
4. p値 < alpha なら H0 を棄却する
5. p値 >= alpha なら H0 を棄却しない
```

数式では、検定統計量を $T_n$ とすると、

$$
p\text{-value}
=
P_{H_0}\left(T_n^{\ast} \ge T_n^{\operatorname{obs}}\right)
$$

です。

| 記号 | 意味 |
|---|---|
| $T_n^{\operatorname{obs}}$ | 実データから計算した検定統計量 |
| $T_n^{\ast}$ | 帰無仮説のもとで発生する統計量 |
| $P_{H_0}$ | 帰無仮説が正しいと仮定した確率 |

---

## 10. 時系列データでの注意点

通常の独立性検定は、サンプルが独立同分布、つまり iid であることを仮定しがちです。

しかし、時系列では、

$$
X_t \not\perp X_{t-1}
$$

のように自己相関があります。

そのため、時系列でCI検定を使うには、次の点に注意が必要です。

- 時間ラグを明示的に特徴量として入れる。
- 自己相関を条件集合で取り除く。
- シャッフル検定を使う場合、単純なランダムシャッフルでは時系列構造を壊す。
- 有効サンプルサイズが見かけの $T$ より小さいことがある。
- 強い自己相関では、検定の偽陽性率や検出力が悪化する。

PCMCIでは、候補リンクを

$$
X^i_{t-\tau} \to X^j_t
$$

のようにラグ付きで扱い、さらに親集合に条件づけることで、自己相関や共通原因を制御しようとします。

---

## 11. PCMCI / FullCIでの読み替え

### 11.1 FullCI

FullCIでは、

$$
X = X^i_{t-\tau},
\quad
Y = X^j_t,
\quad
Z = \mathbf{X}^{-}_{t,\tau_{\max}} \setminus \{X^i_{t-\tau}\}
$$

です。

したがって、検定は

$$
H_0:
X^i_{t-\tau} \perp X^j_t
\mid
\mathbf{X}^{-}_{t,\tau_{\max}} \setminus \{X^i_{t-\tau}\}
$$

です。

これは、

> 候補原因以外の全過去を知ったあとでも、候補原因が結果に追加情報を持つか？

を見ています。

### 11.2 PCMCIのMCI

PCMCIのMCIでは、概念的に

$$
Z
=
\left(\mathcal{P}(X^j_t) \setminus \{X^i_{t-\tau}\}\right)
\cup
\mathcal{P}(X^i_{t-\tau})
$$

を条件集合にします。

検定は、

$$
H_0:
X^i_{t-\tau} \perp X^j_t
\mid
\left(\mathcal{P}(X^j_t) \setminus \{X^i_{t-\tau}\}\right),
\mathcal{P}(X^i_{t-\tau})
$$

です。

FullCIとの違いは、条件集合の大きさです。

| 方法 | 条件集合 |
|---|---|
| FullCI | 候補原因を除く全過去 |
| MCI | 推定された親集合中心 |

MCIは条件集合を小さくすることで、検出力を上げることを狙います。

---

## 12. 「独立」と「非有意」は違う

条件付き独立性検定で非常に重要なのは、次の区別です。

| 結果 | 厳密な意味 | よくある誤読 |
|---|---|---|
| $p < \alpha$ | 条件付き独立という帰無仮説を棄却 | 因果が証明された |
| $p \ge \alpha$ | 条件付き独立を棄却できなかった | 独立が証明された |

どちらの誤読も危険です。

$p < \alpha$ でも、隠れた共通原因、選択バイアス、非定常性、モデルミスがあれば因果とは限りません。

$p \ge \alpha$ でも、サンプルサイズ不足や高次元条件づけによる検出力不足で、真の依存を見落としている可能性があります。

---

## 13. 条件集合を増やせばよいわけではない

直感的には、たくさん条件づければ交絡を取り除けそうに見えます。

しかし、条件集合を増やすことには少なくとも3つのリスクがあります。

### 13.1 検出力低下

条件集合の次元を $k$ とすると、多くの検定では $k$ が増えるほど推定が難しくなります。

偏相関検定なら、自由度は

$$
n-k-2
$$

です。

$k$ が増えると、検出力が下がります。

### 13.2 colliderを条件づけるリスク

colliderを条件づけると、存在しなかった依存を作ることがあります。

```text
X -> Z <- Y
```

では、

$$
X \perp Y
$$

でも、

$$
X \not\perp Y \mid Z
$$

になりえます。

### 13.3 mediatorを条件づけるリスク

直接効果を見たいのか、総効果を見たいのかによって、中間変数を条件づけるべきかが変わります。

```text
X -> Z -> Y
```

で $Z$ を条件づけると、$X$ から $Y$ への総効果の一部を消してしまいます。

因果探索では直接リンクを見たいので条件づけることがありますが、介入効果推定では目的に応じて注意が必要です。

---

## 14. 条件付き独立性検定が因果を意味するための仮定

条件付き独立性検定の結果を因果構造として読むには、追加の仮定が必要です。

| 仮定 | 意味 | 破れるとどうなるか |
|---|---|---|
| 因果Markov条件 | グラフのd-separationが独立性を含意する | グラフから独立性を読めない |
| Faithfulness | 独立性はグラフ構造から来るもので、偶然の係数相殺ではない | 本当はリンクがあるのに独立に見える |
| 因果十分性 | 重要な共通原因が観測されている | 隠れ交絡で偽リンクが出る |
| 正しい時間順序 | 原因は結果より前にある | 向きの解釈が崩れる |
| 定常性 | 関係が時間で大きく変わらない | 平均した検定が意味を失う |
| 適切な最大ラグ | 原因候補のラグが条件集合に含まれる | 遅れた交絡や因果を取り逃がす |

PCMCIやFullCIは、これらの仮定がある程度成り立つときに、条件付き独立性から因果リンクを推定します。

---

## 15. 小さい数値イメージ

次のデータ生成を考えます。

$$
Z \sim \mathcal{N}(0,1)
$$

$$
X = Z + \varepsilon_X
$$

$$
Y = Z + \varepsilon_Y
$$

ここで、$\varepsilon_X, \varepsilon_Y$ は互いに独立なノイズです。

このとき、$X$ と $Y$ はどちらも $Z$ の影響を受けるので、

$$
X \not\perp Y
$$

です。

しかし、$Z$ を知ると、$X$ の残りは $\varepsilon_X$、$Y$ の残りは $\varepsilon_Y$ だけになります。

$$
X - E[X\mid Z] = \varepsilon_X
$$

$$
Y - E[Y\mid Z] = \varepsilon_Y
$$

そしてノイズ同士は独立なので、

$$
X \perp Y \mid Z
$$

です。

偏相関で言えば、残差同士の相関

$$
\operatorname{corr}(\varepsilon_X,\varepsilon_Y)
$$

が0になる、ということです。

---

## 16. PCMCIを読むための最重要ポイント

PCMCIを読むときは、条件付き独立性検定を次のように読むとよいです。

$$
X^i_{t-\tau} \perp X^j_t \mid Z
$$

は、

> 条件集合 $Z$ に入っている過去変数をすでに知っているなら、$X^i_{t-\tau}$ は $X^j_t$ の予測・説明に追加情報を持たない

という意味です。

逆に、

$$
X^i_{t-\tau} \not\perp X^j_t \mid Z
$$

は、

> $Z$ を考慮しても、$X^i_{t-\tau}$ には $X^j_t$ への独自の情報が残っている

という意味です。

PCMCIでは、この「独自の情報」を、時間順序と因果仮定のもとで、ラグ付き因果リンクの証拠として扱います。

ただし、条件付き独立性検定だけで因果が証明されるわけではありません。

---

## 17. 誤解しやすい点まとめ

| 誤解 | 正しい理解 |
|---|---|
| 条件付き独立性検定で因果が証明される | 検定は統計的依存を見る。因果解釈には追加仮定が必要。 |
| $p \ge \alpha$ なら独立が証明された | 棄却できなかっただけ。検出力不足の可能性がある。 |
| 条件づける変数は多いほどよい | 高次元化、collider bias、mediator調整の問題がある。 |
| 相関が消えたら因果がない | 直接効果はないかもしれないが、間接効果や検出力不足の可能性がある。 |
| 非線形CI検定なら万能 | 高次元・少サンプルでは推定が難しい。 |

---

## 18. FullCI / PCMCIとの接続

FullCIは、条件付き独立性検定の条件集合として

$$
Z_{\operatorname{FullCI}}
=
\mathbf{X}^{-}_{t,\tau_{\max}} \setminus \{X^i_{t-\tau}\}
$$

を使う方法です。

PCMCIのMCIは、条件集合として

$$
Z_{\operatorname{MCI}}
=
\left(\mathcal{P}(X^j_t) \setminus \{X^i_{t-\tau}\}\right)
\cup
\mathcal{P}(X^i_{t-\tau})
$$

を使う方法です。

つまり、FullCIとMCIの違いは、条件付き独立性検定そのものというより、

> 何を条件集合 $Z$ として選ぶか

にあります。

この観点で読むと、PCMCIの狙いは、

> 因果的に必要な条件集合だけを選び、高次元化を避けながら条件付き独立性検定を行う

ことだと理解できます。

---

## 19. 出典・確認元

- Runge, J. et al. (2019). `Detecting causal associations in large nonlinear time series datasets`. Science Advances. arXiv:1702.07007. https://arxiv.org/abs/1702.07007
- 条件付き独立性の標準的定義: $X \perp Y \mid Z$ iff $p(x,y\mid z)=p(x\mid z)p(y\mid z)$.
- PCMCI / FullCIとの接続: FullCIは候補原因を除く全過去、MCIは親集合中心の条件集合を用いて条件付き独立性を検定する。

---

## 関連ノート

- [[条件付き独立性]]
- [[FullCI_数式ベース厳密解説|FullCI：数式ベースの厳密解説]]
- [[CMI_GPDC_ParCorr|CMI / GPDC / ParCorr]]
- [[偏相関と検出力]]
- [[PCアルゴリズム]]
- [[検出力と偽陽性]]
- [[次元の呪い]]
- [[../01. 論文読解/03_式から読む|PCMCI 式から読む]]
