 従来の因果構造探索アルゴリズム比較表

作成日: 2026-06-09

## このノートの目的

PCMCIを読む前提として、**PCMCI以外の代表的な因果構造探索アルゴリズム**を一覧で整理するノートです。

ここでいう因果構造探索は、観測データから

```text
X → Y
X ← Z → Y
X ↔ Y
```

のような因果グラフを推定しようとする方法です。

PCMCIは時系列向けの条件付き独立性ベース手法ですが、その背景には以下のような従来手法があります。

- 制約ベース：PC, FCI など
- スコアベース：GES, NOTEARS など
- 関数モデルベース：LiNGAM, ANM, RESIT など
- 時系列・予測ベース：Granger causality, VAR-LiNGAM, Transfer Entropy, CCM など

関連ノート:

- [[条件付き独立性]]
- [[PCアルゴリズム]]
- [[解説シート_レジーム_CI検定_Granger因果|解説シート：レジーム・CI検定・Granger因果]]
- [[時系列因果探索]]
- [[../01. 論文読解/04_手法の流れ|PCMCI 手法の流れ]]
- [[../03. 課題と発展研究/05_PCMCIへの批判と検証論文|PCMCIへの批判と検証論文]]

---

## 1. まず全体分類

| 分類 | 代表アルゴリズム | 基本アイデア | 出力 | 得意な場面 | 主な弱点 |
|---|---|---|---|---|---|
| 制約ベース | PC, FCI, RFCI | 条件付き独立性検定を繰り返して、グラフの辺を消したり向きを決める | CPDAG, PAGなど | 統計的仮定が明示的。因果グラフ理論と相性がよい | CI検定の検出力に依存。高次元・小標本に弱い |
| スコアベース | GES, BIC探索, NOTEARS | グラフごとにスコアを計算し、良いグラフを探索する | DAG | 探索問題として実装しやすい。制約ベースより安定な場合がある | 探索空間が大きい。スコアや関数形の仮定に依存 |
| 関数モデルベース | LiNGAM, ANM, RESIT, CAM | 原因から結果への関数形・ノイズ構造の非対称性を使う | DAGまたは因果順序 | 2変数の向きづけに強い場合がある | 関数形・ノイズ分布の仮定が外れると弱い |
| 時系列・予測ベース | Granger, VAR, VAR-LiNGAM, Transfer Entropy | 過去の情報が未来予測を改善するかを見る | ラグ付き因果候補 | 時系列の時間順序を使える | 予測因果と構造因果を混同しやすい |
| 動的システム系 | CCM, convergent cross mapping | 力学系の状態空間再構成から因果を推定する | 方向つき関係 | 非線形力学系・生態系など | 前提が特殊。同期や共通駆動に注意 |
| 連続最適化・ニューラル系 | NOTEARS, DYNOTEARS, NGCなど | DAG制約を連続最適化で表し、勾配法で学習する | DAGまたは時系列グラフ | 高次元・非線形モデルへ拡張しやすい | 学習安定性、正則化、解釈性、仮定に注意 |

---

## 2. 代表アルゴリズム一覧表

| アルゴリズム | 分類 | 対象データ | 何を仮定するか | 何をするか | 出力の読み方 | PCMCIとの関係・比較 |
|---|---|---|---|---|---|---|
| PC algorithm | 制約ベース | i.i.d.データが基本 | 因果十分性、Causal Markov条件、Faithfulness、非巡回性 | 条件付き独立性検定で辺を削除し、向きを規則で決める | CPDAG。DAGそのものではなく、同値類を表す | PCMCIのPC1ステップの元になった考え方。時系列自己相関にはそのままだと弱い |
| FCI | 制約ベース | i.i.d.データが基本 | 潜在交絡を許す。Faithfulnessなど | 条件付き独立性検定で、潜在交絡を含む可能性を考慮して探索する | PAG。未観測交絡や向き不確実性を含む | LPCMCIなど、潜在交絡対応時系列手法の背景にある |
| RFCI | 制約ベース | i.i.d.データが基本 | FCIに近いが計算を軽くする | FCIより少ない検定で近似的にPAGを推定する | PAG | 高次元でFCIが重い場合の妥協案 |
| SGS | 制約ベース | i.i.d.データ | 因果十分性など | 条件付き独立性からグラフを復元する古典的手法 | DAG/同値類 | PC以前の古典的制約ベース手法として位置づく |
| GES | スコアベース | i.i.d.データ | スコアが真の構造を選ぶ、一貫性など | 辺の追加・削除を貪欲探索し、BICなどのスコアを改善する | CPDAG | CI検定ではなくスコア最適化。PCMCIとは発想が違う |
| Greedy hill-climbing BN search | スコアベース | i.i.d.データ | ベイズネットワーク構造、スコア関数 | 辺の追加・削除・反転でスコアを最大化 | DAG | 局所解に注意。PCMCIよりモデル選択寄り |
| NOTEARS | 連続最適化 | i.i.d.データ | DAG制約を連続関数で表せる、線形または非線形モデル | 非巡回性制約つき最適化問題としてDAGを学習 | DAG | グラフ探索を連続最適化に置き換える。時系列版としてDYNOTEARSがある |
| LiNGAM | 関数モデルベース | i.i.d.データ | 線形、非ガウスノイズ、非巡回、潜在交絡なしが基本 | 非ガウス性を使って因果順序を識別する | DAG | 非ガウス性があると同値類を超えて向きを決められる。線形性が強い制約 |
| DirectLiNGAM | 関数モデルベース | i.i.d.データ | LiNGAMと同様 | 因果順序を直接推定するLiNGAM系手法 | DAG | 線形・非ガウスのとき強い。PCMCIの非線形CI検定とは別方向 |
| ICA-LiNGAM | 関数モデルベース | i.i.d.データ | 線形非ガウス、独立ノイズ | ICAで独立成分を分離して因果構造を推定 | DAG | LiNGAMの代表的古典形 |
| ANM | 関数モデルベース | i.i.d.データ、2変数から多変数へ | $Y=f(X)+N$、原因とノイズが独立 | 両方向に回帰し、残差独立性が成り立つ方向を原因とみなす | 方向つき因果候補 | 非線形関数の非対称性を使う。PCMCIのCI検定とは別の識別原理 |
| RESIT | 関数モデルベース | i.i.d.多変量 | 加法ノイズモデル、非巡回性 | 各変数を他変数で回帰し、残差独立性から原因順序を探す | DAG | ANMを多変量へ拡張したような発想 |
| CAM | 関数モデルベース/スコア系 | i.i.d.多変量 | 加法モデル、スパース性 | 加法回帰と変数選択でDAGを推定 | DAG | 非線形だが加法性を仮定する |
| IGCI | 関数モデルベース | 主に2変数 | 原因分布と生成メカニズムの独立性 | 原因と結果の分布形状の非対称性を見る | 2変数の向き | 多変量ネットワーク推定より、2変数方向づけ向き |
| Granger causality | 時系列・予測ベース | 時系列 | 原因は結果に時間的に先行し、予測を改善する | $X$の過去を入れると$Y$の予測誤差が減るかを見る | ラグ付き予測因果 | PCMCIと同じく時間順序を使うが、標準形は予測・VAR寄り |
| VAR Granger | 時系列・予測ベース | 多変量時系列 | 線形VAR、定常性 | VAR係数が0かどうかを検定する | ラグ付き線形因果候補 | PCMCIのParCorr版と近い直感を持つが、条件集合選択の考え方が違う |
| VAR-LiNGAM | 時系列・関数モデル | 多変量時系列 | 線形VAR、非ガウスノイズ、瞬時効果を扱える場合あり | VARとLiNGAMを組み合わせてラグ付き・同時刻関係を推定 | 時系列DAG/ラグ付き構造 | 非ガウス性で同時刻方向づけに強み。線形性が制約 |
| SVAR / structural VAR | 時系列・経済系 | 多変量時系列 | 線形、識別制約、定常性など | VARの誤差相関を構造ショックとして分解 | 構造的ショックの関係 | 経済時系列でよく使う。識別制約は外部知識に依存しやすい |
| Transfer Entropy | 時系列・情報量 | 時系列 | 情報の流れを条件付き相互情報量で測る | $X$の過去が$Y$の未来に与える追加情報量を測る | 非線形Granger的な情報流 | CMIと近い情報量の発想。高次元では推定が難しい |
| CCM | 動的システム系 | 非線形力学系の時系列 | 同じ力学系上の変数は状態空間に埋め込まれる | 片方の時系列の状態空間から他方を再構成できるかを見る | 非線形力学系の因果候補 | 生態系などで有名。PCMCIとは前提がかなり違う |
| TE + conditioning variants | 時系列・情報量 | 多変量時系列 | 条件付き情報量推定が可能 | 他変数の過去を条件づけたTransfer Entropyを見る | 条件付き情報流 | PCMCIのCMI検定に近いが、グラフ探索手順は別 |
| DYNOTEARS | 連続最適化・時系列 | 多変量時系列 | ラグ付きDAG、線形/拡張モデル、連続DAG制約 | NOTEARSを時系列へ拡張し、ラグ付き構造を最適化する | 時系列グラフ | PCMCIと同じ時系列構造探索だが、CI検定ではなく最適化ベース |
| Neural Granger Causality | ニューラル・時系列 | 多変量時系列 | ニューラル予測モデル、スパース性 | ニューラルネットの入力寄与や正則化からGranger因果を推定 | 非線形予測因果 | 非線形に強いが、構造因果としての解釈には注意 |
| DBN structure learning | 時系列グラフィカルモデル | 離散/連続時系列 | Dynamic Bayesian Network、時間展開DAG | 時刻間・時刻内のベイズネットワーク構造を学習 | 動的グラフ | PCMCIよりベイズネットワーク一般の枠組みに近い |

---

## 3. PCMCIを理解するために重要な従来手法だけ抜き出す

PCMCIを読むうえで特に重要なのは、次の5つです。

| 優先度 | 手法 | なぜ重要か |
|---|---|---|
| 1 | PC algorithm | PCMCIの条件選択ステップの背景。条件付き独立性検定で辺を消す発想の原型。 |
| 2 | Granger causality / VAR | 時系列で「過去が現在を説明するか」を見る基本発想。PCMCIも時間順序を使う。 |
| 3 | FCI | 潜在交絡がある場合、DAGではなくPAGを出す必要があるという背景。LPCMCI理解に必要。 |
| 4 | LiNGAM / VAR-LiNGAM | 非ガウス性を使うと、同時刻因果の向きづけが可能になる場合がある。PCMCI+との比較に重要。 |
| 5 | Transfer Entropy / CMI系 | 非線形・情報量ベースの依存検出。PCMCIのCMI検定を理解する背景。 |

---

## 4. 分類ごとの詳しい見方

### 4.1 制約ベース：PC, FCI, RFCI

制約ベース手法は、条件付き独立性を使います。

基本の問いは、

$$
X \perp Y \mid Z
$$

かどうかです。

もし $Z$ で条件づけると $X$ と $Y$ が独立になるなら、直接の辺 $X - Y$ は不要かもしれない、と判断します。

#### PC algorithm

PC algorithmは、最初に完全グラフから始めて、条件付き独立性が見つかった辺を消していきます。

```text
完全グラフ
  ↓ CI検定で辺を削除
骨格 graph
  ↓ v-structure などの規則で向きづけ
CPDAG
```

出力は通常、DAGそのものではなくCPDAGです。

CPDAGとは、同じ条件付き独立性を持つDAGの集合、つまりマルコフ同値類を表すグラフです。

#### FCI

FCIは、未観測の共通原因、つまり潜在交絡を許します。

PCでは、観測変数だけで因果十分だと仮定しがちですが、FCIでは

```text
U → X
U → Y
```

のような未観測 $U$ の可能性を考えます。

そのため出力はDAGではなくPAGになります。

PAGには、

```text
X o→ Y
X ↔ Y
X o-o Y
```

のように、不確実な端点や潜在交絡の可能性が表されます。

#### PCMCIとの関係

PCMCIは、PC algorithmの考え方を時系列用に改造したものとして読むと理解しやすいです。

ただし、PCMCIは単にPCを時系列に適用するのではなく、

- 自己相関がある
- ラグがある
- 高次元になりやすい
- 条件集合が大きくなりやすい

という時系列特有の問題に対応するため、PC1とMCIという形で条件集合を工夫しています。

---

### 4.2 スコアベース：GES, BIC探索, NOTEARS

スコアベース手法は、グラフ $G$ に対してスコアを定義します。

$$
\operatorname{Score}(G; D)
$$

ここで、

| 記号 | 意味 |
|---|---|
| $G$ | 候補グラフ |
| $D$ | データ |
| $\operatorname{Score}$ | そのグラフがデータをどれくらいうまく説明するか |

たとえばBICなら、ざっくり

$$
\operatorname{BIC}(G)
= \log L(G;D) - \frac{d_G}{2}\log n
$$

のように、当てはまりと複雑さのバランスを見ます。

| 記号 | 意味 |
|---|---|
| $L(G;D)$ | グラフ $G$ のもとでの尤度 |
| $d_G$ | パラメータ数 |
| $n$ | サンプルサイズ |

#### GES

GESはGreedy Equivalence Searchです。

1. 辺を追加してスコアを改善する。
2. その後、辺を削除してさらにスコアを改善する。
3. 貪欲に良いCPDAGを探す。

という流れです。

#### NOTEARS

NOTEARSは、DAG制約を連続最適化で扱う方法です。

DAGであること、つまり閉路がないことを、連続的な制約

$$
h(W) = 0
$$

として表し、重み行列 $W$ を最適化します。

直感的には、

> グラフ探索を組合せ最適化ではなく、ニューラルネットや回帰と同じような連続最適化問題に変える

方法です。

#### PCMCIとの関係

PCMCIは「CI検定で辺を消す」側ですが、GESやNOTEARSは「スコアや損失関数を最適化する」側です。

---

### 4.3 関数モデルベース：LiNGAM, ANM, RESIT

関数モデルベース手法は、原因から結果への生成式に非対称性があることを利用します。

#### LiNGAM

LiNGAMは、Linear Non-Gaussian Acyclic Modelです。

モデルは、ざっくり

$$
X = B X + e
$$

です。

| 記号 | 意味 |
|---|---|
| $X$ | 変数ベクトル |
| $B$ | 因果係数行列 |
| $e$ | 独立な外生ノイズ |

LiNGAMの重要な仮定は、ノイズ $e$ が**非ガウス**であることです。

線形ガウスモデルでは、多くの場合、因果方向を一意に決められません。しかし、非ガウス性があると、独立成分分析の考え方により因果方向を識別できる場合があります。

#### ANM

ANMはAdditive Noise Modelです。

$$
Y = f(X) + N_Y
$$

かつ

$$
N_Y \perp X
$$

が成り立つ方向を、$X \to Y$ と考えます。

逆方向に

$$
X = g(Y) + N_X
$$

と回帰したとき、$N_X \perp Y$ が成り立たないなら、$X \to Y$ のほうが自然だと判断します。

#### RESIT

RESITは、ANMの発想を多変量に広げた方法です。

各変数を他の変数で回帰し、残差が他の変数と独立になりやすいものを原因側として並べます。

#### PCMCIとの関係

PCMCIは条件付き独立性を検定しますが、LiNGAMやANMは「原因方向ではノイズが独立になる」という関数モデル上の非対称性を使います。

そのため、LiNGAMやANMは向きづけに強い場合がありますが、仮定が外れると危険です。

---

### 4.4 時系列・予測ベース：Granger, VAR, Transfer Entropy

時系列では、時間順序を使えることが大きな利点です。

#### Granger causality

Granger causalityは、

> $X$ の過去を入れると $Y$ の予測が改善するなら、$X$ は $Y$ をGranger因果する

という考え方です。

典型的には、

$$
Y_t
= \sum_{\ell=1}^{p} a_\ell Y_{t-\ell}
+ \sum_{\ell=1}^{p} b_\ell X_{t-\ell}
+ \varepsilon_t
$$

で、

$$
H_0: b_1=b_2=\cdots=b_p=0
$$

を検定します。

#### Transfer Entropy

Transfer Entropyは、情報量ベースのGranger causalityに近いです。

$$
TE_{X \to Y}
= I(X_{t}^{\text{past}}; Y_{t+1} \mid Y_t^{\text{past}})
$$

これは、

> $Y$自身の過去を知ったうえで、$X$の過去が$Y$の未来について追加でどれだけ情報を持つか

を測ります。

#### PCMCIとの関係

PCMCIも、過去変数 $X^i_{t-\tau}$ が現在変数 $X^j_t$ に追加情報を持つかを見ます。

ただしPCMCIは、単純な予測改善ではなく、条件付き独立性検定と条件集合選択を組み合わせます。

---

### 4.5 動的システム系：CCM

CCMはConvergent Cross Mappingです。

非線形力学系では、ある変数の時系列に、システム全体の状態情報が埋め込まれることがあります。

たとえば $X$ が $Y$ に因果影響する場合、$Y$ の時系列には $X$ の情報が反映されるため、$Y$ の状態空間から $X$ を再構成できる、という発想です。

直感的には少し逆向きに感じます。

```text
X → Y なら、Y の軌道に X の痕跡が残る
```

そのため、$Y$ から $X$ をうまく推定できるなら、$X \to Y$ を示唆する、という読み方になります。

#### PCMCIとの関係

CCMは力学系・状態空間再構成に強く依存するため、PCMCIやPCとは前提がかなり違います。

---

## 5. どの手法をいつ見るべきか

| 目的 | 見るべき手法 | 理由 |
|---|---|---|
| PCMCIの基礎を理解したい | PC algorithm | PCMCIの条件選択の原型だから |
| 潜在交絡が気になる | FCI, RFCI, LPCMCI | DAGではなくPAGで不確実性を表せるから |
| 同時刻因果の向きが気になる | LiNGAM, VAR-LiNGAM, PCMCI+ | 非ガウス性や同時刻向きづけ規則を使うから |
| 非線形の2変数方向づけを見たい | ANM, RESIT, IGCI | 関数とノイズの非対称性を使うから |
| 時系列の予測因果をまず見たい | Granger, VAR | 実装しやすく、基準線になるから |
| 非線形時系列の情報流を見たい | Transfer Entropy | 条件付き相互情報量に近いから |
| 非線形力学系として扱いたい | CCM | 状態空間再構成の発想が使えるから |
| 最適化・深層学習で構造を学びたい | NOTEARS, DYNOTEARS, NGC | 連続最適化やニューラルモデルで拡張できるから |

---

## 6. PCMCIと従来手法の位置づけ

PCMCIは、かなりざっくり言うと、次のような位置づけです。

```text
PC algorithm
  ↓ 時系列向けに改造
  ↓ 自己相関・高次元・ラグ付き因果に対応
PCMCI
```

ただし、PCMCIは万能ではありません。

| PCMCIが改善した点 | まだ残る課題 | 関連する従来・後続手法 |
|---|---|---|
| 高次元時系列で条件集合を絞る | 潜在交絡 | FCI, LPCMCI |
| 自己相関下でのラグ付き因果検出 | 同時刻因果 | PCMCI+, VAR-LiNGAM |
| 非線形CI検定を選べる | CI検定の推定不安定性 | CMI, GPDC, Transfer Entropy |
| Granger的発想をグラフ探索へ拡張 | 非定常性・レジーム変化 | Regime-PCMCI, change point methods |
| 観測時系列から因果候補を出す | 真の構造因果の保証 | 介入、自然実験、専門家知識 |

---

## 7. 読むときの注意：アルゴリズム名より「仮定」を見る

因果構造探索では、アルゴリズム名よりも、次の仮定を見るほうが重要です。

| チェック項目 | 問い |
|---|---|
| 因果十分性 | 未観測の共通原因はないと仮定しているか？ |
| Faithfulness | 独立性が偶然のパラメータ打ち消しで生じていないと仮定しているか？ |
| 非巡回性 | フィードバックループを許しているか？ |
| 線形性 | 関係は線形と仮定しているか？ |
| ノイズ分布 | ガウス性・非ガウス性・独立性などを仮定しているか？ |
| 定常性 | 時間で構造が変わらないと仮定しているか？ |
| サンプルサイズ | CI検定や密度推定に十分なサンプルがあるか？ |
| 時間分解能 | 因果過程より観測間隔が粗すぎないか？ |

---

## 8. 超短い暗記用まとめ

| 手法 | 一言で覚える |
|---|---|
| PC | 条件付き独立性で辺を消す古典的手法 |
| FCI | 潜在交絡ありのPC系手法 |
| GES | スコアを良くするグラフを貪欲探索 |
| NOTEARS | DAG探索を連続最適化にする |
| LiNGAM | 線形・非ガウス性で向きを決める |
| ANM | 原因方向では残差が原因と独立になる |
| Granger | 過去を入れて未来予測が改善するか |
| Transfer Entropy | 情報量版Granger |
| CCM | 力学系の状態空間再構成で因果を見る |
| DYNOTEARS | NOTEARSの時系列版 |

---

## 9. TODO

- TODO: PC, FCI, GES, LiNGAM, Grangerのそれぞれについて、1ページずつ詳細ノートを作る。
- TODO: PCMCI論文の比較実験で登場する `FullCI`, `MultCI`, `Granger causality` との対応表を追加する。
- TODO: Tigramiteで使えるPCMCI以外の手法・独立性検定を確認する。

---

## 関連ノート

- [[条件付き独立性]]
- [[PCアルゴリズム]]
- [[CMI_GPDC_ParCorr|CMI / GPDC / ParCorr]]
- [[解説シート_レジーム_CI検定_Granger因果|解説シート：レジーム・CI検定・Granger因果]]
- [[時系列因果探索]]
- [[../01. 論文読解/04_手法の流れ|PCMCI 手法の流れ]]
- [[../03. 課題と発展研究/05_PCMCIへの批判と検証論文|PCMCIへの批判と検証論文]]
