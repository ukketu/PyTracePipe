
# 解説シート：レジーム・CMI/GPDC/ParCorr・Granger causality

作成日: 2026-06-09

## このノートの目的

PCMCI周辺の論文を読むときに詰まりやすい単語・検定方法を、**数式を見ながら読める**ように整理するノートです。

対象語:

- レジームとは
- CMI / GPDC / ParCorr の検定方法
- Granger causality とは

関連:

- [[CMI_GPDC_ParCorr|CMI / GPDC / ParCorr]]
- [[条件付き独立性]]
- [[偏相関と検出力]]
- [[自己相関とラグ]]
- [[時系列因果探索]]
- [[../03. 課題と発展研究/05_PCMCIへの批判と検証論文|PCMCIへの批判と検証論文]]

---

## 1. レジームとは

### 1.1 一言でいうと

**レジーム**とは、時系列データの背後にある「状態」「モード」「局面」のことです。

たとえば、同じ変数どうしでも、状態が変わると因果関係が変わることがあります。

```text
平常時レジーム:      X が増えると Y も増える
異常時レジーム:      X が増えると Y は減る
エルニーニョ期:      海面水温と降雨の関係が強い
ラニーニャ期:        その関係が違う
景気拡大局面:        金利と株価の関係がある形で出る
景気後退局面:        同じ関係が弱くなる、または逆転する
```

ここでの「平常時」「異常時」「エルニーニョ期」「ラニーニャ期」「景気拡大局面」「景気後退局面」がレジームです。

---

### 1.2 PCMCIでなぜ重要か

PCMCIは通常、観測期間全体で同じ因果構造が成り立つと仮定します。つまり、ざっくり言えば、

```text
全期間で X → Y の関係が同じ
```

とみなします。

しかし、レジームがある場合、実際にはこうかもしれません。

```text
レジーム 1: X → Y が正の効果
レジーム 2: X → Y が負の効果
```

これを全期間でまとめて分析すると、正の効果と負の効果が打ち消し合って、PCMCIが

```text
X と Y に因果リンクなし
```

と判断してしまう可能性があります。

---

### 1.3 数式で見るレジーム

通常の時系列構造方程式を、かなり単純化して書くと、

$$
Y_t = f(X_{t-1}) + \varepsilon_t
$$

です。

これは「$Y_t$ は、過去の $X_{t-1}$ から影響を受ける」という意味です。

レジームがある場合は、関数 $f$ が状態ごとに変わります。

$$
Y_t = f_{R_t}(X_{t-1}) + \varepsilon_t
$$

記号の意味:

| 記号 | 意味 |
|---|---|
| $Y_t$ | 時刻 $t$ の結果変数 |
| $X_{t-1}$ | 1時点前の説明変数・原因候補 |
| $\varepsilon_t$ | ノイズ、誤差、観測されない揺らぎ |
| $R_t$ | 時刻 $t$ のレジーム、状態、モード |
| $f_{R_t}$ | レジーム $R_t$ に応じて変わる関数 |

たとえば、$R_t = 1$ のときは、

$$
Y_t = 2X_{t-1} + \varepsilon_t
$$

で、$R_t = 2$ のときは、

$$
Y_t = -2X_{t-1} + \varepsilon_t
$$

だとします。

このとき、レジーム1では $X$ は $Y$ を増やし、レジーム2では $X$ は $Y$ を減らします。全期間を混ぜると、平均的には効果がゼロに見えるかもしれません。

---

### 1.4 誤解しやすい点

#### 誤解1：レジームは必ず観測できる

必ずしも観測できません。むしろPCMCIで問題になるのは、レジームが**未観測の背景状態**として存在する場合です。

```text
観測しているもの: X, Y, Z
本当は効いているもの: 季節、政策変更、気候状態、装置モード、個体状態
```

この未観測レジームは、潜在変数や潜在交絡のように振る舞います。

#### 誤解2：レジームは単なるカテゴリ変数

カテゴリ変数として扱える場合もありますが、本質は「データ生成メカニズムが変わること」です。

カテゴリ名があるかどうかより、次の問いが重要です。

> 同じ変数どうしの関係式が、時期や状態によって変わっていないか？

---

## 2. CMI / GPDC / ParCorr は何を検定しているのか

### 2.1 まず共通の目的

PCMCIでやりたいのは、候補リンク

$$
X^i_{t-\tau} \to X^j_t
$$

について、条件付き独立性を調べることです。

つまり、

$$
X^i_{t-\tau} \perp X^j_t \mid \mathcal{S}
$$

かどうかを検定します。

これは日本語で言うと、

> 条件集合 $\mathcal{S}$ をすでに知っているとして、それでも $X^i_{t-\tau}$ と $X^j_t$ の間に関係が残るか？

という問いです。

ここで、

| 記号 | 意味 |
|---|---|
| $X^i_{t-\tau}$ | 変数 $i$ の $\tau$ 時点前の値。原因候補。 |
| $X^j_t$ | 変数 $j$ の現在値。結果候補。 |
| $\mathcal{S}$ | 条件づけ集合。他の親候補や自己ラグなど。 |
| $\perp$ | 独立である、関係がない。 |
| $\mid$ | 「〜で条件づけたとき」。 |

CMI, GPDC, ParCorr は、この条件付き独立性を調べるための別々の道具です。

---

## 3. ParCorr：偏相関による線形の条件付き独立性検定

### 3.1 一言でいうと

**ParCorr** は partial correlation、つまり**偏相関**を使う検定です。

直感的には、

> 条件変数 $\mathcal{S}$ で説明できる部分を $X$ と $Y$ の両方から取り除き、残り同士が相関しているかを見る。

という方法です。

---

### 3.2 数式の流れ

候補原因を $X$、結果を $Y$、条件集合を $Z$ と書きます。

PCMCI文脈では、たとえば

$$
X = X^i_{t-\tau}, \quad Y = X^j_t, \quad Z = \mathcal{S}
$$

です。

まず $X$ を $Z$ で線形回帰します。

$$
X = a_X^\top Z + r_X
$$

次に $Y$ も $Z$ で線形回帰します。

$$
Y = a_Y^\top Z + r_Y
$$

記号の意味:

| 記号 | 意味 |
|---|---|
| $a_X$ | $Z$ から $X$ を予測する回帰係数 |
| $a_Y$ | $Z$ から $Y$ を予測する回帰係数 |
| $r_X$ | $Z$ では説明できなかった $X$ の残差 |
| $r_Y$ | $Z$ では説明できなかった $Y$ の残差 |

その後、残差同士の相関を計算します。

$$
\rho_{XY \cdot Z}
= \operatorname{corr}(r_X, r_Y)
$$

この $\rho_{XY \cdot Z}$ が 0 に近ければ、

$$
X \perp Y \mid Z
$$

に近いと考えます。

逆に、$\rho_{XY \cdot Z}$ が 0 から十分離れていれば、

$$
X \not\perp Y \mid Z
$$

つまり「$Z$ で条件づけても $X$ と $Y$ の関係が残る」と判断します。

---

### 3.3 検定統計量

サンプルサイズを $n$、条件変数の次元を $k$ とします。偏相関係数を $r = \rho_{XY \cdot Z}$ とすると、典型的には次の $t$ 統計量を使います。

$$
t
= r \sqrt{\frac{n - k - 2}{1 - r^2}}
$$

帰無仮説は、

$$
H_0: \rho_{XY \cdot Z} = 0
$$

です。

意味:

- $H_0$：条件づけたあと、$X$ と $Y$ に線形相関は残っていない。
- $H_1$：条件づけたあとも、$X$ と $Y$ に線形相関が残っている。

---

### 3.4 ParCorrの強みと弱み

| 項目 | 内容 |
|---|---|
| 得意 | 線形関係。サンプルがそれほど多くなくても比較的安定。 |
| 苦手 | 非線形関係。たとえばU字型、しきい値型、複雑な相互作用。 |
| PCMCIでの意味 | 線形近似で十分なら、速くて扱いやすいCI検定。 |

---

## 4. GPDC：非線形回帰で残差を作り、距離相関を見る

### 4.1 一言でいうと

**GPDC** は Gaussian Process Distance Correlation の略です。

直感的には、ParCorrの非線形版です。

ParCorrでは、$Z$ で説明できる部分を**線形回帰**で取り除きました。

GPDCでは、$Z$ で説明できる部分を**ガウス過程回帰**で非線形に取り除きます。そのうえで、残差同士の依存を**距離相関**で見ます。

---

### 4.2 数式の流れ

また、候補原因を $X$、結果を $Y$、条件集合を $Z$ とします。

GPDCでは、まず次のような非線形回帰を考えます。

$$
X = f_X(Z) + r_X
$$

$$
Y = f_Y(Z) + r_Y
$$

ここで、

| 記号 | 意味 |
|---|---|
| $f_X$ | $Z$ から $X$ を予測する非線形関数 |
| $f_Y$ | $Z$ から $Y$ を予測する非線形関数 |
| $r_X$ | 非線形に見ても $Z$ では説明できない $X$ の残差 |
| $r_Y$ | 非線形に見ても $Z$ では説明できない $Y$ の残差 |

ガウス過程回帰では、$f_X$ や $f_Y$ を固定の直線ではなく、関数の分布として扱います。

ざっくり書くと、

$$
f_X \sim \mathcal{GP}(0, K)
$$

です。

記号の意味:

| 記号 | 意味 |
|---|---|
| $\mathcal{GP}$ | Gaussian Process、ガウス過程 |
| $K$ | カーネル関数。入力 $Z$ の近さに応じて関数値がどれくらい似るかを決める。 |

ここで重要なのは、ガウス過程が「直線に限らない柔らかい回帰」をしてくれる点です。

---

### 4.3 距離相関とは

GPDCでは、残差 $r_X$ と $r_Y$ の依存を、普通の相関ではなく**距離相関**で見ます。

普通の相関は主に線形関係に敏感です。一方、距離相関はより一般的な依存関係を拾えます。

距離相関の細かい定義は複雑ですが、直感的には、

> $r_X$ 同士が近いサンプルでは、$r_Y$ 同士も近くなるか？

を見ています。

簡略化して書くと、距離相関は

$$
\operatorname{dCor}(r_X, r_Y)
= \frac{\operatorname{dCov}(r_X, r_Y)}
{\sqrt{\operatorname{dVar}(r_X)\operatorname{dVar}(r_Y)}}
$$

です。

| 記号 | 意味 |
|---|---|
| $\operatorname{dCor}$ | distance correlation、距離相関 |
| $\operatorname{dCov}$ | distance covariance、距離共分散 |
| $\operatorname{dVar}$ | distance variance、距離分散 |

これが 0 に近ければ、残差同士は独立に近いと考えます。

---

### 4.4 GPDCの帰無仮説

GPDCでも帰無仮説は同じです。

$$
H_0: X \perp Y \mid Z
$$

ただし、実際には

1. $X$ から $Z$ の非線形効果を取り除く。
2. $Y$ から $Z$ の非線形効果を取り除く。
3. 残差 $r_X$ と $r_Y$ が依存しているかを距離相関で見る。

という手順で検定します。

---

### 4.5 GPDCの強みと弱み

| 項目 | 内容 |
|---|---|
| 得意 | 加法的な非線形関係。ParCorrでは見逃す曲線的な関係。 |
| 苦手 | 高次元・短いサンプル。ガウス過程回帰の計算コスト。 |
| PCMCIでの意味 | 「線形ではなさそうだが、CMIほど一般的な推定は重い」というときの中間的な選択肢。 |

---

## 5. CMI：条件付き相互情報量による一般的な依存検定

### 5.1 一言でいうと

**CMI** は Conditional Mutual Information、条件付き相互情報量です。

直感的には、

> $Z$ を知ったあとで、$X$ を知ることが $Y$ について追加でどれくらい情報を与えるか？

を測ります。

もし $Z$ を知った時点で $Y$ の情報が十分に説明されていて、$X$ を追加で知っても何も増えないなら、

$$
I(X;Y \mid Z) = 0
$$

です。

---

### 5.2 数式定義

条件付き相互情報量は、確率密度を使うと次のように書けます。

$$
I(X;Y \mid Z)
= \int p(x,y,z)
\log \frac{p(x,y \mid z)}{p(x \mid z)p(y \mid z)}
\, dx\, dy\, dz
$$

または同値に、

$$
I(X;Y \mid Z)
= \int p(x,y,z)
\log \frac{p(x,y,z)p(z)}{p(x,z)p(y,z)}
\, dx\, dy\, dz
$$

です。

記号の意味:

| 記号 | 意味 |
|---|---|
| $p(x,y,z)$ | $X, Y, Z$ の同時確率密度 |
| $p(x \mid z)$ | $Z=z$ のときの $X$ の条件付き密度 |
| $p(y \mid z)$ | $Z=z$ のときの $Y$ の条件付き密度 |
| $p(x,y \mid z)$ | $Z=z$ のときの $X,Y$ の同時条件付き密度 |
| $\log$ | 対数。情報量の単位を決める。 |

---

### 5.3 この式の読み方

中心はこの比です。

$$
\frac{p(x,y \mid z)}{p(x \mid z)p(y \mid z)}
$$

もし $Z$ で条件づけたあとに $X$ と $Y$ が独立なら、

$$
p(x,y \mid z) = p(x \mid z)p(y \mid z)
$$

なので、比は 1 になります。

$$
\log 1 = 0
$$

したがって、全体の積分も 0 になります。

つまり、

$$
X \perp Y \mid Z
\quad \Longleftrightarrow \quad
I(X;Y \mid Z) = 0
$$

です。

逆に、$Z$ で条件づけても $X$ と $Y$ が依存していれば、

$$
I(X;Y \mid Z) > 0
$$

になります。

---

### 5.4 CMIの帰無仮説

CMI検定の帰無仮説は、

$$
H_0: I(X;Y \mid Z) = 0
$$

です。

これは、

$$
H_0: X \perp Y \mid Z
$$

と同じ意味です。

対立仮説は、

$$
H_1: I(X;Y \mid Z) > 0
$$

です。

つまり、

> $Z$ で条件づけても、$X$ と $Y$ の間に情報的な依存が残っている。

という意味です。

---

### 5.5 CMIの推定が難しい理由

CMIは非常に一般的ですが、実データでは確率密度 $p(x,y,z)$ を知らないので、サンプルから推定する必要があります。

特に $Z$ の次元が高いと、

$$
p(x,y,z)
$$

を安定して推定するのが難しくなります。これが**次元の呪い**です。

PCMCIが条件選択をして $\mathcal{S}$ を小さくしようとするのは、CMIのような検定を安定させるためでもあります。

---

### 5.6 CMIの強みと弱み

| 項目 | 内容 |
|---|---|
| 得意 | 線形・非線形を問わない一般的な依存。 |
| 苦手 | 高次元条件づけ、短いサンプル、密度推定の不安定性。 |
| PCMCIでの意味 | 最も一般的だが、使えば万能というより、十分なデータ量と低次元条件集合が必要。 |

---

## 6. 3つの検定の違いを一枚で見る

| 検定 | 何をするか | 見える関係 | 数式の中心 | 弱点 |
|---|---|---|---|---|
| ParCorr | $Z$ の線形効果を取り、残差相関を見る | 線形依存 | $\rho_{XY \cdot Z}=\operatorname{corr}(r_X,r_Y)$ | 非線形に弱い |
| GPDC | $Z$ の非線形効果をガウス過程で取り、残差の距離相関を見る | 加法的非線形依存 | $\operatorname{dCor}(r_X,r_Y)$ | 計算が重い、高次元に弱い |
| CMI | $Z$ を知ったあと、$X$ が $Y$ に与える追加情報量を見る | 一般的依存 | $I(X;Y \mid Z)$ | 密度推定が難しい |

### 読み方の目安

- まず線形で十分そうなら **ParCorr**。
- 非線形だが、回帰で説明できる形を想定するなら **GPDC**。
- より一般的な依存を拾いたいなら **CMI**。
- ただし、CMIは一般的なぶん、サンプルサイズと次元に厳しい。

---

## 7. Granger causalityとは

### 7.1 一言でいうと

**Granger causality** とは、

> $X$ の過去を知ることで、$Y$ の未来予測が改善するなら、$X$ は $Y$ をGranger因果する

という考え方です。

重要なのは、Granger causality は本来、**予測可能性にもとづく因果概念**だという点です。

---

### 7.2 数式で見る

まず、$Y_t$ を自分自身の過去だけで予測するモデルを考えます。

$$
Y_t
= \sum_{\ell=1}^{p} a_\ell Y_{t-\ell}
+ \varepsilon_t^{(0)}
$$

次に、$X$ の過去も入れて予測します。

$$
Y_t
= \sum_{\ell=1}^{p} a_\ell Y_{t-\ell}
+ \sum_{\ell=1}^{p} b_\ell X_{t-\ell}
+ \varepsilon_t^{(1)}
$$

記号の意味:

| 記号 | 意味 |
|---|---|
| $Y_t$ | 予測したい変数の現在値 |
| $Y_{t-\ell}$ | $Y$ の $\ell$ 時点前の値 |
| $X_{t-\ell}$ | $X$ の $\ell$ 時点前の値 |
| $p$ | 最大ラグ、何時点前まで使うか |
| $a_\ell$ | $Y$ の過去の係数 |
| $b_\ell$ | $X$ の過去の係数 |
| $\varepsilon_t^{(0)}$ | $Y$ の過去だけで予測したときの誤差 |
| $\varepsilon_t^{(1)}$ | $X$ の過去も入れて予測したときの誤差 |

もし $X$ の過去を入れることで予測誤差が小さくなるなら、

$$
\operatorname{Var}(\varepsilon_t^{(1)})
<
\operatorname{Var}(\varepsilon_t^{(0)})
$$

となります。

このとき、$X$ は $Y$ をGranger因果すると言います。

---

### 7.3 仮説検定としてのGranger因果

線形VARモデルでは、帰無仮説を次のように置きます。

$$
H_0: b_1 = b_2 = \cdots = b_p = 0
$$

これは、

> $X$ の過去は $Y$ の予測に追加的に役立っていない。

という意味です。

対立仮説は、

$$
H_1: \text{少なくとも1つの } b_\ell \ne 0
$$

です。

これは、

> $X$ の過去のどこかが、$Y$ の予測に役立っている。

という意味です。

---

### 7.4 Granger causalityと「本当の因果」の違い

Granger causalityは便利ですが、注意が必要です。

$X$ の過去が $Y$ の予測に役立つからといって、必ずしも

$$
X \to Y
$$

という介入的・構造的な因果があるとは限りません。

たとえば、未観測の $U$ が両方を動かしている場合、

```text
U_{t-1} → X_t
U_{t-1} → Y_t
```

$X$ の過去が $Y$ の予測に役立つように見えることがあります。

また、サンプリング間隔が粗い場合や、共通トレンドがある場合にも、Granger因果が見かけ上出ることがあります。

---

## 8. PCMCIとGranger causalityの関係

### 8.1 共通点

PCMCIもGranger causalityも、時系列の「過去から現在へ」という時間順序を利用します。

どちらも、ざっくり言えば、

> 過去の $X$ が、現在の $Y$ に追加情報を持つか？

を見ています。

---

### 8.2 違い

| 観点 | Granger causality | PCMCI |
|---|---|---|
| 基本発想 | 予測が改善するか | 条件付き独立性が破れるか |
| 典型的モデル | 線形VAR | CI検定ベース。ParCorr, GPDC, CMIなどを選べる |
| 条件づけ | 他変数の過去を入れるが、モデル依存 | PC1で親候補を絞り、MCIで条件づける |
| 非線形 | 標準形は線形 | CI検定を変えれば非線形対応可能 |
| 出力の読み方 | 予測的因果 | 条件付き独立性にもとづく因果候補 |

---

### 8.3 PCMCI的に言い換えると

Granger causalityは、かなり大ざっぱには、

$$
X_{t-1}^{i} \not\perp X_t^j \mid \text{過去の情報}
$$

を調べる考え方に近いです。

PCMCIでは、この「過去の情報」を全部入れるのではなく、PC1ステップで重要そうな条件集合を絞り、MCIで候補リンクを検定します。

そのため、PCMCIはGranger causalityの単純な拡張というより、

> 時系列Granger的な発想を、条件付き独立性検定とグラフ探索で精密化した方法

として読むと理解しやすいです。

---

## 9. 超短い復習

### レジーム

時系列の背後にある状態・局面。レジームが変わると因果構造や効果の向きが変わることがある。

### ParCorr

条件変数で線形回帰した残差同士の相関を見る。速くて安定だが、線形関係に寄る。

### GPDC

条件変数で非線形回帰した残差同士の距離相関を見る。非線形に強いが、重くなりやすい。

### CMI

条件変数を知ったあと、$X$ が $Y$ について追加で持つ情報量を見る。一般的だが、推定が難しい。

### Granger causality

$X$ の過去を入れると $Y$ の未来予測が改善する、という予測ベースの因果概念。本当の構造因果と同一視してはいけない。

---

## 10. さらに確認したいTODO

- TODO: PCMCI論文中の ParCorr / GPDC / CMIknn の実験図と、このノートの検定説明を対応づける。
- TODO: Tigramite実装で、`ParCorr`, `GPDC`, `CMIknn` の引数と出力p値を確認する。
- TODO: Granger causality と transfer entropy の違いを別ノート化する。

---

## 関連ノート

- [[CMI_GPDC_ParCorr|CMI / GPDC / ParCorr]]
- [[条件付き独立性]]
- [[偏相関と検出力]]
- [[自己相関とラグ]]
- [[時系列因果探索]]
- [[../01. 論文読解/03_式から読む|式から読む]]
- [[../01. 論文読解/04_手法の流れ|手法の流れ]]
- [[../03. 課題と発展研究/05_PCMCIへの批判と検証論文|PCMCIへの批判と検証論文]]
