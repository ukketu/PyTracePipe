# PCMCI論文の実験内容と結果解説

## このノートの目的

このノートでは、PCMCI論文の実験パートを「何を確かめるための実験か」「どういうデータで比較したか」「結果から何が言えるか」に分けて読む。

対象論文：Runge et al., *Detecting and quantifying causal associations in large nonlinear time series datasets*.

根拠箇所：本文の `Model setup for high-dimensional synthetic data experiments`, `Experiments with linear relationships`, `Experiments with nonlinear relationships`, `Causal strength experiments`, `Further experiments` および Supplementary `S4 Model setup / Performance evaluation`。

## 先に結論

この論文の実験で示したい主張は、かなりはっきりしています。

> PCMCIは、高次元・自己相関あり・時間遅れあり・非線形ありの時系列でも、FullCIより検出力を落としにくく、単純相関より偽陽性を抑え、PC単体やLassoよりリンクごとの最悪ケース性能が安定しやすい。

もう少し短く言うと、PCMCIの強みは次の2つです。

1. **条件集合を小さくする**ので、高次元でも検定が壊れにくい。
2. **MCIで親集合に条件づける**ので、相関ではなく直接的な時間遅れリンクを見やすい。

## 実験全体の構造

論文の実験は、大きく次の流れで組まれています。

| 実験 | 目的 | 主な図 | 何を比較するか |
|---|---|---:|---|
| 気候の説明例 | FullCIが検出力を失い、PCMCIが有利になる直感を示す | Fig. 2 | Corr / FullCI / PCMCI |
| 線形合成データ実験 | 高次元・自己相関・ラグありで、線形リンクをどれだけ見つけるか | Fig. 4 | Corr / FullCI / Lasso / PC / PCMCI |
| 非線形合成データ実験 | 非線形リンクでもPCMCIが使えるか | Fig. 5 | dCor / MI / FullCI-GPDC / PC-GPDC / PCMCI-GPDC / FullCI-CMI / PC-CMI / PCMCI-CMI |
| 因果強度実験 | MCIの効果量が因果効果の強さとして読めるか | Fig. 6 | Corr / FullCI partial correlation / MCI partial correlation |
| 追加ロバスト性実験 | サンプルサイズ、結合密度、観測ノイズを変えても結果が崩れないか | Supplementary figures | 複数設定 |

## なぜ合成データを使うのか

実データ、特に気候データでは「本当の因果グラフ」が完全にはわかりません。したがって、手法の性能を定量評価するには、真のリンクが既知であるデータが必要です。

そのため論文では、実データに似た性質を持つ合成時系列を作ります。ここで模倣したい難しさは次の4つです。

1. **高次元性**：変数数 $N$ が大きい。
2. **時間遅れ因果**：$X^i_{t-\tau}$ が $X^j_t$ に影響する。
3. **自己相関**：各変数が自分の過去に強く依存する。
4. **非線形性**：入力と出力の関係が直線ではない場合がある。

## 合成データの生成モデル

Supplementary S4では、合成データはおおむね次の形で生成されます。

$$
X^j_t = a_j X^j_{t-1} + c \sum_i f_i\left(X^i_{t-\tau_i}\right) + \eta^j_t
$$

この式の意味は次の通りです。

| 記号 | 意味 |
|---|---|
| $X^j_t$ | 時刻 $t$ における変数 $j$ の値 |
| $a_j X^j_{t-1}$ | 自己相関。自分の1時刻前からの影響 |
| $a_j$ | 自己相関の強さ。大きいほど過去の自分に引っ張られる |
| $c$ | 因果リンクの係数。リンク強度をそろえるために使う |
| $f_i(\cdot)$ | 線形または非線形の関数 |
| $X^i_{t-\tau_i}$ | 原因候補。変数 $i$ の $\tau_i$ 時刻前の値 |
| $\tau_i$ | ラグ。論文設定では主に 1 または 2 からランダムに選ぶ |
| $\eta^j_t$ | ノイズ。独立なガウスノイズ |

直感的には、各変数は

```text
自分の過去 + 他変数の過去からの因果入力 + ノイズ
```

で作られています。

ここで重要なのは、作った側は「どの $X^i_{t-\tau}$ がどの $X^j_t$ に本当に影響したか」を知っていることです。だから各手法が出したリンクを、真のリンクと照合できます。

## 評価指標

論文では、主に次を見ています。

| 指標 | 日本語 | 意味 |
|---|---|---|
| TPR / true positive rate | 真陽性率、検出力 | 本当にある因果リンクをどれだけ見つけたか |
| FPR / false positive rate | 偽陽性率 | 本当はないリンクをどれだけ誤検出したか |
| FDR | 偽発見率 | 検出したリンクのうち、どれだけが誤りか |
| runtime | 実行時間 | 計算にどれだけ時間がかかるか |
| effect size | 効果量 | 検定統計量や偏相関として見たリンクの強さ |

この論文では、単に平均性能だけでなく、**リンクごとのばらつき**も重視しています。つまり「平均は高いが、あるリンクだけ全然見つからない」手法は弱点ありと見ます。

## 実験1：気候の説明例

### 何を確かめたいか

気候の例では、PCMCIの利点を直感的に示します。特に、Nino指数とBritish Columbia気温の関係を題材にして、次を見ます。

- Corr：単純相関だけだと、共通原因や間接効果も拾ってしまう。
- FullCI：全過去に条件づけるので偽陽性は抑えやすいが、条件集合が大きくなりすぎて検出力が落ちる。
- PCMCI：PC1で必要そうな親だけに絞ってからMCIを行うので、効果量と検出力が残りやすい。

### 結果の読み方

論文では、PCMCIは高次元ケースでもNinoからBCTへのリンクを比較的高い検出力で見つけられる、と説明しています。FullCIは理屈としては正しい条件付き独立性を見るのですが、不要な変数まで条件集合に入るため、偏相関などの効果量が小さくなり、有限サンプルでは検出できなくなります。

この例は「なぜPCMCIが必要か」を見せる導入です。厳密なベンチマークというより、後の合成データ実験の動機づけとして読むのがよいです。

## 実験2：線形合成データ実験

### 実験設定

線形リンクだけを持つ合成時系列で比較します。本文では、サンプル長 $T=150$、最大ラグ $\tau_{\max}=5$ の設定で、ネットワークサイズ $N$ を変化させています。

比較対象は次の通りです。

| 手法 | 何をしているか | 期待される弱点 |
|---|---|---|
| Corr | 単純相関 | 共通原因・間接効果・自己相関で偽陽性が多い |
| FullCI | 全過去を条件集合に入れる条件付き独立性検定 | 高次元で検出力が落ちる |
| Lasso | 正則化回帰で変数選択 | 因果仮定を明示的に扱うわけではなく、リンクごとのばらつきが残る |
| PC-ParCorr | PCアルゴリズム単体 + 偏相関 | 時系列構造への扱いが不十分な場合がある |
| PCMCI-ParCorr | PC1 + MCI + 偏相関 | 提案手法 |

### 結果1：Corrは偽陽性が多い

単純相関は、因果探索としては不十分です。理由は、直接リンク以外でも相関が出るからです。

たとえば、

```text
X -> Z -> Y
```

なら、$X$ と $Y$ の間に直接リンクがなくても相関します。また、

```text
X <- Z -> Y
```

でも、共通原因 $Z$ のせいで $X$ と $Y$ が相関します。

論文では、Corrは偽陽性率が非常に高く、因果発見には不適切だと示されます。

### 結果2：FullCIは高次元で検出力が落ちる

FullCIは「すべての過去変数に条件づける」ので、理論的には偽陽性を抑える方向に働きます。しかし、条件集合が大きすぎると、有限サンプルでは検定が弱くなります。

論文の結果では、FullCIは $N=5$ 程度ならある程度検出できますが、$N=20$ では検出力が大きく落ち、次元がサンプルサイズを超えるような大きな $N$ では適用自体が難しくなります。

ポイントは、FullCIが「間違った考え方」なのではなく、**有限サンプルかつ高次元では条件づけが重すぎる**ということです。

### 結果3：PCMCIは高次元でも検出力を保つ

PCMCIは、PC1で親候補を絞ってからMCI検定をします。そのため、FullCIより条件集合が小さくなります。

本文では、PCMCIはネットワークサイズが大きくなっても高い検出力を示し、特に $N=20$ までは、同じ因果効果を持つリンクに対して自己相関の弱いリンク・強いリンクの間で検出力がほぼ同じになる、と説明されています。

これは重要です。自己相関が強い系列では、単純な相関やPC単体では性能が崩れやすいからです。

### 結果4：Lassoは平均性能は近いが、最悪ケースが弱い

論文では、PCMCIの平均検出力はLassoと近い場合があります。しかし重要な違いとして、Lassoでは一部のリンクが一貫して見落とされることがあります。

本文では、$N=10$ の小さいネットワークでも、Lassoではかなりのリンクが常に見落とされる一方、PCMCIでは 99% のリンクが 70% を超える検出力を持つ、と述べられています。

つまり、PCMCIの主張は「平均で勝つ」だけではありません。

> リンクごとのばらつきが小さく、見落とされ続けるリンクが少ない。

という主張です。

## 実験3：非線形合成データ実験

### 実験設定

非線形実験では、リンク関数 $f_i$ に線形だけでなく非線形関数を混ぜます。Supplementary S4では、非線形モデルではリンクのうち

- 50%：線形 $f^{(1)}(x)=x$
- 25%：非線形 $f^{(2)}(x)$
- 25%：非線形 $f^{(3)}(x)$

とする設定が説明されています。

比較では、線形の偏相関だけでは不十分なので、非線形依存を検出する検定を使います。

| 実装 | 検定 | 直感 |
|---|---|---|
| GPDC | Gaussian Process + distance correlation | GP回帰で条件変数の影響を引いて、残差独立性を見る |
| CMI | conditional mutual information | 条件付き相互情報量で非線形依存を見る |
| dCor / MI単体 | 無条件の依存尺度 | 非線形依存は拾うが、共通原因や間接効果を除けない |

### 結果1：dCorやMI単体は偽陽性が多い

dCorやMIは非線形依存も拾えます。しかし、条件づけをしないため、相関と同じく間接効果や共通原因も拾います。

そのため、非線形リンクを検出できても、「直接因果リンクかどうか」は区別しにくいです。本文では、特に自己相関が強い場合に偽陽性が高くなると説明されています。

### 結果2：FullCI-GPDCは高次元で壊れやすい

GPDCは非線形な条件付き独立性検定として強力ですが、高次元条件集合では難しくなります。FullCIは全過去を条件集合に入れるため、GPDCの苦手な高次元条件づけをそのまま踏みます。

本文では、FullCI-GPDCは $N=5$ 程度まではよく働くが、$N \ge 10$ では偽陽性制御が難しくなる、と説明されています。

### 結果3：PCMCI-GPDCは最高水準の検出力だが、大規模では少し偽陽性が膨らむ

PCMCI-GPDCは、PC1で条件集合を小さくしてからGPDCを使うため、FullCI-GPDCより高次元性の悪影響を避けられます。

本文では、PCMCI-GPDCは最も高い検出力を示し、ネットワークが大きくなっても検出力の低下は小さいとされます。ただし、大きなネットワークでは条件集合がそれでも増えるため、GPDCの高次元での弱さが残り、偽陽性が少し膨らむと述べられています。

ここは読み方が大事です。

> PCMCIはGPDCを魔法のように完全化するのではなく、GPDCが破綻しにくいように条件集合を小さくする。

という理解が正確です。

### 結果4：CMI実装ではFullCIがほぼ検出力を失い、PCMCIがPCより高い検出力を持つ

CMIを用いた完全ノンパラメトリック実装では、FullCIは計算も重く、特に非線形リンクでほとんど検出力を持たないと説明されています。一方で、PCMCIは偽陽性を制御しつつ、PCより高い検出力を示す場合が多いとされます。

CMIは柔軟ですが、次元が上がると推定が難しくなります。したがって、ここでもPCMCIの条件選択が効いています。

## 実験4：因果強度実験

### 何を確かめたいか

この実験では、「MCIの検定統計量は、単にリンクの有無を判定するだけでなく、因果効果の強さとして読めるのか」を調べます。

設定としては、ネットワークサイズ $N=20$ に固定し、リンク係数 $c$ を変化させます。標準化された因果効果は次の形で表されます。

$$
\frac{|c|}{\sqrt{1+c^2}}
$$

この式は、係数 $c$ が大きくなるほど因果効果が大きくなるが、標準化によって $0$ から $1$ の範囲に収まるような量だと読めます。

### 比較される効果量

| 効果量 | 意味 | 問題点 / 利点 |
|---|---|---|
| correlation | 単純相関 | 因果効果と無関係に大きくも小さくもなる |
| FullCI partial correlation | 全過去に条件づけた偏相関 | 条件づけすぎで小さくなりやすい |
| MCI partial correlation | PCMCIの親集合に条件づけた偏相関 | 因果効果に近い量になりやすい |

### 結果

論文では、同じ因果係数 $c$ を持つリンクでも、単純相関は広い範囲にばらつき、ときにはほぼゼロになります。これは、相関が因果効果の強さをうまく反映しないことを示しています。

FullCIの偏相関は、相関よりは分布が狭くなりますが、因果強度より小さくなりがちです。これは不要な条件づけにより、効果量が薄まるためです。

一方、MCIはFullCIより大きな効果量を持ちやすく、論文の補足ではMCIがFullCIより大きくなること、またMCIが因果強度の推定量として解釈できることを議論しています。

この実験の読みどころは、PCMCIの検出力が高い理由を次の2つに分解している点です。

1. 条件集合が小さいので、検定の次元が低い。
2. 効果量がFullCIより大きく残る。

## 追加実験：サンプルサイズ・密度・観測ノイズ

Supplementaryでは、次のような追加設定も調べています。

| 追加設定 | 何を見るか | 結果の要点 |
|---|---|---|
| サンプルサイズ変更 | $T$ が変わっても傾向が同じか | 大きなサンプルでも主結果は概ね頑健 |
| 高い結合密度 | リンクが増えても使えるか | 高密度では難しくなるが、基本傾向を確認 |
| 観測ノイズ | 観測にノイズが混ざったらどうなるか | 小さいノイズでは影響は軽微。ノイズが動的ノイズと同程度になると性能低下し、偽陽性制御も崩れやすい |

特に観測ノイズについては、動的ノイズの標準偏差の25%程度までは小さい影響だが、同程度の大きさになると共通原因の検出が難しくなり、偽陽性制御が悪化すると説明されています。

## 図ごとの読み方まとめ

### Fig. 4：線形実験

見るべきポイントは次です。

1. 上段：真のリンクをどれだけ検出したか。
2. 下段：存在しないリンクをどれだけ誤検出したか。
3. 横軸：ネットワークサイズ $N$。
4. 箱ひげ：リンクごとの検出率の分布。
5. 左右の箱：自己相関が弱いペア / 強いペア。
6. 上部の数字：実行時間。

読み取りの結論は、Corrは偽陽性が多く、FullCIは高次元で検出力が落ち、PCは自己相関に弱い場合があり、PCMCIは偽陽性を抑えつつ高い検出力を維持する、です。

### Fig. 5：非線形実験

見るべきポイントは、非線形リンクを含む場合に、GPDCやCMIのような非線形検定をPCMCIに組み込むとどうなるかです。

結論は、非線形依存を無条件に見るだけのdCor/MIは偽陽性が多く、FullCIは高次元条件づけで苦しくなり、PCMCIは条件集合を絞ることで非線形検定を比較的うまく使える、です。

ただし、GPDCもCMIも高次元には弱いので、ネットワークが大きいと完全ではありません。

### Fig. 6：因果強度

見るべきポイントは、リンク係数 $c$ を変えたとき、各効果量が真の因果強度にどれだけ対応するかです。

相関は、同じ因果効果でも大きくばらつきます。FullCIは因果効果より小さくなりがちです。MCIはFullCIより大きく、因果強度に近い量として解釈しやすい、というのが主張です。

## この実験結果から言えること

### 言えること

- PCMCIは、時系列の高次元因果探索でFullCIより検出力が高くなりやすい。
- PCMCIは、相関・dCor・MI単体より偽陽性を抑えやすい。
- PCMCIは、PC単体より時系列の自己相関構造に対応しやすい。
- MCIの効果量は、リンクのランキングや因果強度の解釈に使いやすい。
- GPDCやCMIを組み込むことで、非線形リンクにも対応できる。

### 言いすぎてはいけないこと

- 「PCMCIが常に真の因果を発見する」とは言えない。
- 隠れ共通原因、非定常性、観測ノイズが大きい状況では崩れうる。
- GPDCやCMIは高次元で完全に安定するわけではない。
- 実データで真の因果グラフが既知なわけではないので、実応用の図は検証ではなく整合性の例として読むべき。

## 自分用の理解

この論文の実験は、PCMCIの勝ち方を次のように整理するとわかりやすいです。

```text
FullCI:
  条件づけは正攻法
  しかし全過去を入れるので次元が高すぎる
  → 効果量が小さくなる
  → 検出力が落ちる

Corr / dCor / MI:
  依存は拾える
  しかし条件づけない
  → 共通原因・間接効果も拾う
  → 偽陽性が多い

PC単体:
  条件付き独立性で削る
  しかし時系列自己相関への扱いが不十分な場合がある
  → 強い自己相関で性能が崩れることがある

PCMCI:
  PC1で親候補を高検出力寄りに残す
  MCIで必要な親集合に条件づける
  → 条件次元が下がる
  → 効果量が残る
  → 偽陽性を抑えつつ検出力が上がる
```

## 輪講で聞かれそうな質問

### Q1. なぜFullCIは理論的にはよさそうなのに性能が悪いのか？

A. すべての過去変数に条件づけるため、条件集合の次元が大きくなり、有限サンプルでは条件付き独立性検定の検出力が落ちるからです。不要な変数にまで条件づけると、効果量も小さくなります。

### Q2. PCMCIはなぜFullCIより検出力が高いのか？

A. PC1で候補親を絞り、MCIでは主に関連する親集合だけに条件づけるからです。これにより検定の次元が下がり、MCIの効果量もFullCIより大きく残りやすくなります。

### Q3. なぜ相関ではダメなのか？

A. 相関は直接因果リンクだけでなく、共通原因・間接経路・自己相関による依存も拾うからです。したがって偽陽性が多くなります。

### Q4. 非線形でもPCMCIは使えるのか？

A. 使えます。ただし、ParCorrではなくGPDCやCMIのような非線形条件付き独立性検定を組み込む必要があります。実験ではPCMCI-GPDCやPCMCI-CMIが比較されています。

### Q5. GPDCやCMIを使えば非線形問題は完全に解けるのか？

A. 完全ではありません。GPDCやCMIは高次元条件集合で推定が難しくなります。PCMCIは条件集合を小さくすることでこの問題を緩和しますが、大きなネットワークでは偽陽性が少し膨らむ場合があります。

### Q6. MCIの値は因果効果そのものなのか？

A. 論文では、MCIは仮想的介入に対応する因果強度として解釈できる、と議論しています。ただし、これは因果十分性、正しい時間順序、Markov性、faithfulnessなどの仮定の上での解釈です。

## TODO

- TODO: Supplementary Fig. S3/S4/S8/S10/S12/S13を個別に読み、どの設定変更に対応するか表にする。
- TODO: Fig. 4/5の箱ひげ図を、実際の図を見ながら「どの箱がどの手法か」まで注釈する。
- TODO: MCIがFullCIより効果量が大きくなる補足証明を、[[03_式から読む|式から読む]]側で展開する。

## 関連ノート

- [[00_索引|PCMCI論文 読解索引]]
- [[02_図から読む|図から読む]]
- [[03_式から読む|式から読む]]
- [[04_手法の流れ|手法の流れ]]
- [[../02. 前提知識解説/CMI_GPDC_ParCorr|CMI / GPDC / ParCorr]]
- [[../02. 前提知識解説/GPDCと独立性検定が確率モデルになる理由|GPDCと独立性検定が確率モデルになる理由]]
- [[../03. 課題と発展研究/01_仮定と限界|仮定と限界]]
